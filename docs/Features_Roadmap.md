# Features Roadmap — aa-findocs

> **Ver também:** [`docs/CONTEXT.md`](./CONTEXT.md) — contexto completo do projeto, stack, decisões técnicas e regras de operação do Claude.

## ✅ Concluídos
- **Bank Bonds — sync cria compras + current value por bond + Holdings mostra current value + Bond Matching removido — ENTREGUE (jul/2026, v1.13.0):** 4 pedidos do usuario. **(1)** Sync do SimpleFin agora cria transacoes `buy` de bonds novos (`bondBuyTransactions` em `lib/simplefin-map.js`, ID sintetico, custo = `purchase_price x shares` do feed com fallback `cost_basis`/`market_value`, staging em `pendingFid`) — terceira forma de entrada junto de manual + CSV. **(2)** Card "Bond Matching" removido de `src/Transactions.jsx` (resolucao 100% automatica por `descKey` agora). **(3)** Position Performance casa o Current Value individualmente por bond via novo helper `computeBankBondsMarketValue` (`src/lib/bankBonds.js`, market value por descKey, fallback pro custo quando o SimpleFin nao retorna o bond). **(4)** Tab Holdings exibe o current value do SimpleFin como valor principal do `bank-bonds-aggregate` (cost + gain/loss no bloco de referencia). 7 testes novos. Ver `docs/CONTEXT.md` secao "Bank Bonds — sync cria compras + current value por bond".
- Holdings card compacto: 2 linhas por ativo, separador fino, estilo Yahoo Finance
- **Item 3** (mai/2026): Fix overlap legendas eixo X — `angle={-45}`, `textAnchor="end"`, `height={60}`, ticks calculados por `computeXAxis()` alinhados a fronteiras de calendário
- **Item 4** (mai/2026): Renomeado "Portfolio Value" / "Current Value" → "Net Worth" em toda a tab Performance
- **Item 5** (mai/2026): Título do gráfico adicionado — dinâmico: "Net Worth Growth" (modo USD) ou "Portfolio VS S&P 500" (modo comparação)
- **Item 6** (mai/2026): Tooltip do gráfico com data completa via `labelFormatter` (ex: "May 1, 2024")
- **Item 7** (mai/2026): Seção Diagnostics removida do Performance.jsx
- **Item 2** (mai/2026): Eye Toggle em Performance — prop `valuesHidden` do App.jsx. Oculta só Net Worth e tooltip; percentuais sempre visíveis. Eixo Y colapsa de 64→16px quando oculto.
- **Item 9** (mai/2026): Eye Toggle em Transactions — prop `valuesHidden` do App.jsx. Oculta colunas Price e Fee na tabela.
- **Item 10** (mai/2026): Scroll horizontal isolado na tabela de Transactions — wrapper `overflowX: auto` + `minWidth: 760px`. Header renomeado "Tkr"→"Ticker". Padding Notes corrigido (2→10px).
- **Item 1** (mai/2026): Tabela "Position Performance" em `Performance.jsx` — PR30→35. Avg cost + gain/loss por ticker a partir de transações × preço atual. Ativos BR incluídos via fxRate. 8 colunas clicáveis com sort (default: Current Value desc). Linha TOTAL fixa no topo. Coluna Ticker sticky. "Group by class" com subtotais e grupos colapsáveis. Asset class lida das próprias transações.
- **Import preview editável** (jun/2026): Preview do ImportModal (CSV + Fidelity) com checkbox por linha (default: todas marcadas), header checkbox select/deselect all, inline edit por double-click, botão mostra "Import X of Y rows", só linhas marcadas são importadas.
- **Item 14** (jun/2026 — PR #37): ETFs Fixed Income → auto-classificar como `Bonds`. Lista `FIXED_INCOME_ETFS` (25 tickers) em `Transactions.jsx`. Aplicado em 4 lugares: form, parser CSV, parser Fidelity, backfill on load.
- **Item 15** (jun/2026 — PR #37): REITs e ETFs Real Estate → auto-classificar como `Real Estate`. Lista `REAL_ESTATE_ETFS` (10 tickers). Mesma função `inferAssetClass()` do item 14.
- **Item 16** (jun/2026 — PR #37 + #39): Todos asset classes no Performance exceto Cash. `INCLUDED_CLASSES` expandido para incluir `Bonds`, `Bank Bonds`, `BRA Fixed Income`. Tickers sem candles ignorados silenciosamente. Cache bumped v10→v11. Net Worth KPI unificado com fonte live de Position Performance.
- **Item 13** (jun/2026): US Bank Bonds live — pesquisa de API gratuita por CUSIP/ticker concluída. Implementado.
- **Item 11** (jun/2026 — PRs #41–#50): BRA Fixed Income (Tesouro Direto) — tentativas de live pricing via Brapi `/treasury` (403 pago), tesourodireto.com.br (410 descontinuado), CKAN datastore (400 desativado). Pivotou para **entrada manual em BRL com conversão automática para USD**. Holdings `BRA Fixed Income` aceitam `manualCurrency: "BRL"`, convertido via `usdBrlRate` (`GET /api/price?fx=USDBRL`, cascata Finnhub → open.er-api → Frankfurter, cacheado em localStorage). Seletor USD/BRL no form de add/edit. Tickers `tesouro-*` e classe `BRA Fixed Income` pulam validação de ticker.
- **Item 12** (jun/2026 — PRs #41–#50): `BRA Fixed Income` como asset class BR para Tesouro/CDB — holdings manuais com suporte a valor em BRL. CDB Banco Guanabara permanece manual em BRL até vencimento (out/2028).
- **Bug fix — Performance não reage a mudanças em Transactions** (jun/2026): Cache Redis de `perf-history` usava key fixa por usuário, ignorando mudanças nas transactions. Fix: cache key agora inclui FNV-1a hash das transactions elegíveis (`id|date|side|ticker|qty|price`). Qualquer adição, remoção ou edição gera hash diferente → cache miss → recalcula automaticamente. Cache bumped v11→v12.
- **Bug fix — "Bank Bonds" ausente na mensagem de erro de classe elegível** (jun/2026): Mensagem "No transactions in eligible asset classes" em Performance.jsx não listava Bank Bonds. Corrigido.
- **Fix — Disclaimer Performance.jsx** (jun/2026): Texto "Excludes fixed income & unallocated assets" já estava corrigido no código para "Excludes Cash and Unallocated assets". Roadmap e CONTEXT.md atualizados para refletir.
- **Pesquisa de fontes de dados — Tab Dividends** (jun/2026 — PR #58): Probe validou fontes. Yahoo `chart?events=div` para US (keyless, gratuito); brapi `dividends=true` rejeitado (HTTP 403, feature paga); Finnhub `/stock/dividend` rejeitado (premium). BRA Stocks, BRA Fixed Income e Bank Bonds → entrada manual. Income model: `totalReceived` direto. Storage: `portfolio:<storageKey>:income-manual`.
- **Item 17** (jun/2026): Income History — card colapsavel com KPIs (All Time / YTD / This Month) e bar chart com seletor de view `Month | Quarter | Half | Year`. Implementado em `src/Dividends.jsx`.
- **Item 18** (jun/2026): Bar chart com agrupamentos por periodo (Month/Quarter/Half/Year) dentro do Income History card. Implementado junto com item 17.
- **Item 25** (jun/2026): Calculadora de aporte mensal → split quinzenal. Inputs: valor fixo mensal, proventos do mes anterior, venda DELL, entradas extras. Total ÷ 2 = 1a e 2a quinzena. Implementado em `src/AporteQuinzenal.jsx`.
- **Item 26** (jun/2026): Realizado vs Planejado — registro do aporte realizado por quinzena, mostra pendente vs planejado. Implementado em `src/AporteQuinzenal.jsx`.
- **Item 27** (jun/2026): Historico de aportes — bar chart de evolucao dos aportes (mesmo seletor de view `Year | 6M | Quarter | Month`). Implementado em `src/AporteQuinzenal.jsx`.
- **Item 19 — y/y nos KPIs do Income History** (jun/2026 — PR #62): KpiCard YTD e This Month exibem variacao percentual ano-a-ano abaixo do valor principal. `priorYtd` e `priorMonth` calculados no useMemo `kpis`. Nota: grafico mes anterior vs atual (parte original do item 19) ainda pendente.
- **Dropdown de anos no Income History** (jun/2026 — PR #62): Substituiu inputs de date range (From/To) por `<select>` de anos. Opcoes: "All years" + anos presentes nos dados em ordem decrescente. Simplifica o filtro para selecao de ano inteiro.
- **Group by Asset Class em Position Dividends** (jun/2026 — PR #62): Toggle "By Ticker" / "By Asset Class" na tabela Position Dividends. Quando "By Asset Class": agrega dividendos por classe (Stocks, Real Estate, etc.) derivando a classe das transactions. Header sticky muda de "Ticker" para "Class".
- **Item 19 (restante) — Comparador Mes Anterior vs Mes Atual** (jun/2026 — PR #64): Bloco "Month vs Month" inserido no topo do card "Income History" em `Dividends.jsx`. Dois cards lado a lado — "Prev Month" (mes anterior completo) e "This Month" (acumulado ate hoje) — com delta percentual MoM centralizado (verde/vermelho) e nomes dos meses por extenso. Oculto quando o usuario filtra por ano historico diferente do corrente. Reutiliza `useMemo kpis` existente sem nenhum novo fetch ou mudanca de API.
- **Item 24 — Comparador Y/Y por mes com diferenca por asset** (jun/2026 — PR #65): `buildYoyData(events)` agrupa eventos de dividendos por ticker e mes para o ano atual vs ano anterior. `YearVsYearTable` — card colapsavel abaixo do Income History com tickers como linhas, meses como colunas; cada celula exibe valor do ano atual + valor do ano anterior (muted) + indicador de delta (tri/tri + %) em verde/vermelho. Linha TOTAL no rodape, scroll horizontal no mobile, mensagem de empty state. Apenas `src/Dividends.jsx` alterado.
- **Item 23 — Colunas Div TTM + YoC% na Position Performance** (jun/2026 — PR #67): Fetch paralelo de `POST /api/dividends` dentro de `PerformanceView` via `Promise.allSettled` (degradacao silenciosa). Estado `divByTicker` construido a partir de `e.totalReceived`, filtrando TTM por ultimos 365 dias. Duas novas colunas na `PositionPerformanceTable`: `Div TTM` (USD, sujeito a `valuesHidden`) e `YoC %` (TTM / totalCost x 100, sempre visivel). Tickers sem dados exibem `--`. `aggFromRows` atualizado: `divTtmSum` somado, `yoc` agregado como media ponderada (`sum(ttm) / sum(totalCost)`). `minWidth` da tabela bumpeado de 860px para 1060px. Apenas `src/Performance.jsx` alterado.
- **Item 8 — Total Return chart** (jun/2026 — PR #68): Terceira linha "Total Return" (cor `T.green`) no grafico de comparacao; KPI card "Total Return {period}" entre Portfolio e S&P 500; estado `divEvents` com array bruto de eventos; `totalReturn = portfolio[d] + (cumulativeDivsUSD / initialPortfolioUSD) x 100` acumulando apenas dividendos dentro do periodo selecionado; `undefined` (nao `null`) em pontos sem dado para recharts pular silenciosamente; degradacao silenciosa se fetch falhar; apenas US dividends cobertos (BRA e fixed income excluidos); apenas `src/Performance.jsx` alterado.
- **Item 30 — Sort + filter + TOTAL row no Dividend History** (jun/2026 — PR #71): `DivHistPopover` local (mesmo padrão do `HeaderPopover` de Transactions). Date → date range; Ticker → checkboxes; $/Share, Qty, Total → sort only. TOTAL row fixa no topo, soma linhas visíveis após filtragem. Contador no header do card muda para "X / Y payments" quando filtrado. `useRef` adicionado ao import do `Dividends.jsx`. Apenas `src/Dividends.jsx` alterado.
- **Item 31 — DELL dividendos verificado** (jun/2026): Confirmado sem mudança de código. `api/dividends.js` não exclui DELL. Exclusão em `AporteQuinzenal.jsx` (linha 139) aplica-se apenas ao gráfico de aportes (vesting ≠ aporte); dividendos DELL fluem normalmente para Dividend History, Year vs Year e Total Return.
- **Item 34 — Import inteligente (reuso de classe + dedupe)** (jun/2026): Duas validações novas no preview do `ImportModal` (CSV + Fidelity), só em `src/Transactions.jsx`. **(1) Reuso de classe conhecida:** `parseRow` e `parseFidelityCSV` recebem um `knownClassByTicker` (Map ticker→assetClass construído das transações salvas via `useMemo`). Prioridade: coluna `assetClass` explícita → classe do histórico → `inferAssetClass()` → pede manual. Resultados ganham flag `classFromHistory`; chip "N class reused" no header do preview. **(2) Detecção de duplicata:** helper module-level `dupKey(tx)` = `ticker|side|qty|date`; `dupKeySet` dos salvos via `useMemo`. `annotateDuplicates()` marca `r.duplicate = true` nas linhas que batem. Linhas duplicadas vêm **desmarcadas por padrão** (`initAllChecked` pula duplicatas), fundo vermelho suave e label "Duplicate" — usuário pode re-marcar pra forçar (`r.ok` permanece true). Chip "N duplicate". `commitPreviewEdit` recalcula `duplicate` após edição inline.
- **Fix — dividendos bucket por pay date via Polygon.io** (jun/2026 — PR #73): Yahoo retorna só ex-date; `api.nasdaq.com` bloqueado pelo Akamai a partir dos IPs do Vercel (403). Polygon.io (`v3/reference/dividends`) é a fonte de pay date. qtyHeld calculado na ex-date (entitlement); `date` do evento = pay date (fallback gracioso para ex-date). Cache permanente por ticker no Redis (global, TTL 7 dias, warm de 5 por request). Cache resultado persiste só quando todos os tickers estão warm. Cache bumped v1→v3. Requer `POLYGON_API_KEY` no Vercel.
- **NET QTY total row em Transactions + auto-detecção de formato de data no import CSV** (jun/2026 — PR #77): **(1) NET QTY row:** `tfoot` adicionado à `TransactionTable` com linha "NET QTY" = `sum(buy qty) − sum(sell qty)` sobre as linhas visíveis pós-filtro. Label "NET QTY" na coluna Ticker, valor líquido na coluna Qty, cor verde/vermelho. Útil ao filtrar por ticker para ver posição líquida. **(2) Auto-detecção de formato de data:** nova função `detectDateFormat(dateStrings)` varre todos os valores da coluna `date` antes de parsear: em `A/B/YYYY`, se algum `A > 12` → DMY; se algum `B > 12` → MDY. Default MDY (US/Excel) quando ambíguo. Datas ISO (`YYYY-MM-DD`) tratadas por ramo separado em `parseDate`, independente do `fmt`. `parseDate` aceita `fmt: "mdy" | "dmy"` (default "mdy"). `parseRow` e `reparseWithCommaDecimal` recebem `opts = { dateFormat }`. `parsed.dateFormat` armazenado no state do `ImportModal`. Chip dourado "dates: DD/MM/YYYY detected" aparece no preview apenas quando DMY é detectado.
- **Item 29 — Redesign "Dividends Monthly Y/Y"** (jun/2026): Card "Year vs Year" redesenhado e renomeado para "Dividends Monthly Y/Y". **(1) Month selector:** dropdown `<select>` com todos os meses com dados (CY ou PY); default = mês mais recente com dados. **(2) Tabela simplificada:** linhas = assets, colunas = PY (ano anterior, cor muted) · CY (ano atual) · Δ $ (CY − PY, verde/vermelho) · Δ % (CY/PY − 1, verde/vermelho). `fmtDeltaUSD` helper com prefixo `+`. Linha TOTAL fixa no topo. **(3) Agrupamento:** default "By Class" (deriva asset class das transactions via `tickerToClass` useMemo); toggle "By Ticker" para view flat. **(4) UX:** layout idêntico aos demais cards colapsáveis; empty state por mês; footer "CY vs PY · Month. Δ = CY minus PY." Apenas `src/Dividends.jsx` alterado.
- **Item 41** (jun/2026): Reordenacao de cards em `src/Dividends.jsx`. Ordem final: (1) Income History, (2) Position Dividends, (3) Dividends Monthly Y/Y, (4) Dividend History. Mudanca de layout puro, sem alteracao de logica.
- **Filtros de ticker e asset class no Income History** (jun/2026 — PR #89): Dois dropdowns single-select adicionados ao card Income History em `src/Dividends.jsx`. Filtro por ticker (todos os tickers unicos dos eventos, ordenados) e por asset class (todas as classes unicas, ordenadas). Afetam apenas o bar chart — KPIs, Position Dividends e Dividend History nao sao afetados. Indicador visual: borda dourada no `<select>` quando o filtro nao esta em "All". Puramente local, sem novo fetch ou mudanca de API.
- **Fidelity bulk import: tab padrao + selecao de multiplos CSVs** (jun/2026 — PR #90): O modal de bulk import abre por default na aba "Fidelity" (era "Upload CSV"). O file picker da aba Fidelity aceita `multiple` arquivos. Os resultados de todos os arquivos sao mergeados, deduplicados cross-arquivo via `dupKey` e ordenados por data antes de exibir o preview. Apenas `src/Transactions.jsx` alterado.
- **Item 42** (jun/2026): Default do month selector em "Dividends Monthly Y/Y" = mes corrente (`new Date().getMonth() + 1`) se presente nos dados; caso contrario, manter o mes mais recente com dados. Apenas `src/Dividends.jsx`.
- **Item 43** (jun/2026): Group by Asset Class em "Dividends Monthly Y/Y" com grupos colapsaveis (mesmo padrao do Position Performance). State `collapsedClasses` (Set), `toggleClass`, `classGroups` useMemo, `renderGroupHeaderRow` com ChevronDown rotacionado. Default collapsed ao ativar "By Class". Apenas `src/Dividends.jsx`.
- **Itens 20, 21, 22 — Tab Events** (jun/2026 — commit aecef28): Novo endpoint `api/events.js` (310 linhas) + novo componente lazy-loaded `src/Events.jsx` (390 linhas) + 4 mudancas cirurgicas em `App.jsx` (import lazy, caso "events" no H1, tab entre Dividends e Transactions, bloco Suspense). Janela de eventos: hoje-30d a hoje+90d, ordenado por data asc. Tipos cobertos: `ex_dividend` (Yahoo primario, Finnhub fallback para ADRs), `payout` (quando payDate Polygon difere da ex-date, reutilizando cache de pay dates da Tab Dividends), `split`/`reverse split` (Yahoo primario, Polygon fallback), `earnings` (Finnhub primario, Yahoo fallback). Filtro client-side por tipo (pills All | Ex-Div | Payout | Earnings | Split). Agrupamento cronologico: Last 7 Days, Last Month, Today, This Week, Next Week, depois Mon YYYY. Cache Redis GLOBAL por hash de tickers (sem storageKey) — eventos de calendario sao fatos publicos. BRA Stocks fora do escopo (sem API gratuita de earnings/splits). Build verde (chunk Events 7.21 kB).
- **Item 28** (jun/2026 — PR #98): Reconciliacao automatica plano x realizado na Tab Aporte Quinzenal. Funcao pura `computeHalfInvested(transactions, usdBrlRate, year, month)` calcula o total investido por quinzena (1a: dias 1-15; 2a: dias 16-fim) a partir de transacoes `buy`, excluindo DELL, com conversao BRL->USD via `txToUSD`. Campo "Invested" do card "This Month" substituido de `<input>` manual (salvo em localStorage) por `<span>` derivado via `useMemo([transactions, usdBrlRate])`. Estado `realizado`/`setRealizado`, `loadRealizado`, `currentMonthKey` e inputs manuais removidos. `localStorage.removeItem(LS_REALIZADO)` no mount limpa a chave legada.
  - **PR #99 (jun/2026) — Fix destructuring + DELL sale auto + dividendos mes anterior auto:** (1) Bug fix: `computeHalfInvested` retornava `{ half1, half2 }` mas o `useMemo` destructurava `{ half1Auto, half2Auto }` — ambos eram `undefined`. Fix: `useMemo` agora extrai os campos corretos. (2) Nova funcao pura `computeDellSale(transactions, usdBrlRate, year, month)` soma sells de DELL do mes corrente; campo "DELL sale" no Monthly Plan virou display read-only com label "from Transactions". (3) Apos load das transactions, dispara `POST /api/dividends` e filtra eventos do mes anterior, somando `totalReceived`; campo "Dividends (last month)" virou display read-only com label "from Dividends" (falha silenciosa mostra $0.00). Apenas "Monthly fixed amount" e extras permanecem como inputs manuais.
- **Position Dividends — By Class colapsavel com chevron** (jun/2026 — PR #92): `PositionDividendsTable` atualizada para trabalhar como `YearVsYearTable`. Quando "By Asset Class" e selecionado, linhas sao agrupadas por classe com chevron colapsavel (default = collapsed). `buildClassGroups()` computa subtotais por grupo; `collapsedClasses` state + `toggleClass` handler; `renderGroupHeaderRow` com ChevronDown animado (rotaciona -90deg quando collapsed). Grupo expandido exibe tickers individuais ordenados por total desc. Funcao `buildAssetClassRows` (modo flat legado) removida. Apenas `src/Dividends.jsx`.
- **DIV TTM / YoC% para Bank Bonds na Position Performance** (jun/2026 — PR #93): Confirmado ja implementado — nenhum codigo novo necessario. `Performance.jsx` ja tinha `loadTransactions` retornando `{ transactions, bondIncome }`, os helpers `buildBondEvents` e o merge de bond events. VALE (BRA stock) exibe `--` corretamente (sem API gratuita de dividendos BRA).
- **Toggle "By Class / By Ticker" movido para o header dos cards** (jun/2026 — PR #93): Aplicado nos 3 cards afetados — Position Performance (`Performance.jsx`), Position Dividends e Dividends Monthly Y/Y (`Dividends.jsx`). Botoes de toggle movidos do corpo do card para o header `<button>`, alinhados a direita. `e.stopPropagation()` impede que o click no toggle feche/abra o card. Default "By Class" com grupos collapsed ao ativar; `useEffect` com `[groupMode]` dep auto-colapsa grupos em `YearVsYearTable` ao mudar para "class".
- **Lock de valor e asset class para Bank Bonds no modal de edicao de Holdings** (jun/2026 — PR #93): Em `ManualHoldingRow` (`App.jsx`), guard `isBankBonds = assetClass.includes("Bank Bonds") || derivedFromTransactions === true`. Para holdings Bank Bonds: input de valor, input de asset class, botao remove e popup de class edit sao ocultados/desabilitados. `saveEdit` pula patch de `manualValue`/`assetClass`/`assetClassOverride` quando `isBankBonds`. Target% pode continuar sendo editado normalmente.
- **Bug fix — VALE (ADR) sem dividendos no Dividends tab** (jun/2026 — PR #94): Yahoo Finance retorna HTTP 200 com `dividends: {}` vazio para alguns ADRs US-listados (VALE NYSE confirmado) — falha silenciosa, sem erro. Fix: `fetchFinnhubDividends(ticker, apiKey)` adicionado em `api/dividends.js` como fallback quando Yahoo retorna null ou vazio; chama Finnhub `/stock/dividend` (reutiliza `FINNHUB_API_KEY` existente); Finnhub inclui `payDate` diretamente, entao lookup Polygon e pulado para esses eventos. `AUTO_CLASSES` ja havia sido estendido no PR #93 para incluir `"BRA Stocks"` — permitindo que tickers US-listados com essa classe (ex: VALE) passem pelo filtro de fetch. `isBrazilianTicker` continua bloqueando tickers reais da B3 (VALE3, etc.). `CACHE_VERSION` bumpeado `v4 → v5` para invalidar caches vazios anteriores.
- **Bug fix — VALE e outros tickers US com dividendos via Fidelity import** (jun/2026 — PR #95): Root cause definitivo do VALE: `parseFidelityCSV` descartava explicitamente todas as linhas `DIVIDEND RECEIVED` / `CASH DIV` com `else continue`. Fix em 4 arquivos: **(1) `src/Transactions.jsx`:** captura linhas de dividendo antes do bloco de side-detection como `incomeEvents` com `{ id, date, ticker, amount, kind: "dividend", source: "fidelity" }`. UI de import exibe contagem separada: "N bond interest + M stock dividend payments detected." **(2) `src/Dividends.jsx` e `src/Performance.jsx`:** passam `bondIncome` no body do POST `/api/dividends`. **(3) `api/dividends.js`:** processa eventos `kind: "dividend"` de `bondIncome` como fonte autoritativa — usa `totalReceived` exato, pula Yahoo/Finnhub para tickers cobertos; cache key inclui hash Fidelity; `CACHE_VERSION` bumpeado `v5 → v6`. Cobre VALE e qualquer ticker US com dividendos recebidos via Fidelity. Requer que o usuário reimporte o CSV Fidelity.
- **Bug fix — applyTxQty ignorava holdings legados** (jun/2026 — PR #82): Holdings criados antes do campo `type` existir têm `type: undefined`. `applyTxQty` usava `h.type !== "auto"` como guard, excluindo silenciosamente esses holdings — seus qtys nunca sincronizavam mesmo com Refresh All ou reload. Fix: predicate alterado para `h.type === "manual"`, alinhando com a convenção do restante do app. Holdings sem transações (`!(h.ticker in netQty)`) continuam intactos.
- **Item 32** (jun/2026 — PR #81): Qty de auto holdings derivada automaticamente do log de Transactions. Tres pontos de sync: **(1) load** — ao carregar a pagina, busca `/api/transactions`, computa net qty e corrige holdings no Redis se mudou; **(2) Refresh All** — transactions buscadas em paralelo com precos, qty sync aplicado no mesmo `setHoldings` atomico; **(3) live** — `persist()` em `TransactionsView` chama `onTransactionsChange?.(nextList)` apos cada save (add, edit, delete, bulk delete, bulk class, import); `App.jsx` recebe e aplica imediatamente via `handleTransactionsChange`. Holdings sem nenhuma transacao ficam inalterados (backwards-compatible). Input de Quantity removido do form de edicao do `HoldingRow` — qty e somente leitura para auto holdings.
- **Item 33** (jun/2026 — PR #84): Formulario "Add Live Asset" removido da tab Holdings em `src/App.jsx`. Removidos: funcao `handleCSVFile`, bloco JSX da section (~225 linhas) com form colapsavel, inputs Ticker/Quantity/Target%, botao "Add to portfolio", upload CSV, status bar. Todo ativo `type: "auto"` e criado/atualizado exclusivamente via sync com Transactions (item 32). Bundle reduziu de 312.74 kB para 307.89 kB. Holdings `type: "manual"` e Cash continuam sendo adicionados diretamente em Holdings normalmente.
- **Item 39** (jun/2026): Duas partes. **(1) qtyHeld na ex-date — validado:** `qtyAtDate(transactions, ticker, exDate)` em `api/dividends.js` ja calcula corretamente a qty na ex-date para ambos os cenarios: venda entre ex-date e pay-date preserva a posicao da ex-date; compra entre ex-date e pay-date e excluida. **(2) Bug fix — dividendos futuros apareciam no historico:** o Yahoo lista dividendos recem-declarados cuja ex-date ja passou (qty > 0, evento gerado) mas cujo pay date do Polygon ainda esta no futuro. O evento era datado no futuro (`date = payDate || exDate`) e aparecia no Dividend History / KPIs / Total Return mesmo sem o dinheiro ter caido. Fix: guard `if (date > todayISO) continue;` no loop de eventos descarta dividendos ainda nao pagos (futuro estrito; pay date de hoje continua incluido). Contador `futureSkipped` no `meta` para diagnostico. Server-side, cobre Dividends e Performance num so ponto.
- **Item 40** (jun/2026 — PR #86, corrigido em PR #87): Correcao de qty **e** preco de Bank Bonds no parser Fidelity (`src/Transactions.jsx`). Para `assetClass === "Bank Bonds"` (CUSIP), `parseFidelityCSV` aplica `tx.qty = qtyAbs / 1000` (Fidelity reporta Quantity como valor de face em dolares: 1000 = um CD de $1.000) **e** `tx.price = priceN * 10` (Price ($) vem como percent-of-face: 100.00 = 100% de $1.000 = $1.000/unidade). Ex: CSV qty=1000/price=100.00 → tx qty=1/price=1000 → valor $1.000. `rawNumbers` preserva os valores brutos. (Diagnostico do PR #86 aplicava so `price * 10` — incompleto; PR #87 corrige.) Tambem extrai metadados dedicados: `couponRate` (number), `maturityDate` (ISO), `bondType` (Treasury/Agency/CD/Corporate), `shortName`, `couponFreq` (default `monthly`).
- **Item 40 metadados + income real** (jun/2026 — PR #87): novos campos de bond no parser e deteccao de pagamentos reais de juros. Ver "US Bank Bonds — income real" abaixo.
- **Item 37** (jun/2026 — PR #86): Holding agregado "US Bank Bonds" derivado automaticamente de transacoes em `src/App.jsx`. Helpers module-level `computeBankBondsPrincipal(transactions)` (Sigma buy qty*price - Sigma sell qty*price, floored em 0) e `applyBankBondsHolding(...)` mantêm um unico holding manual `id: "bank-bonds-aggregate"`, `assetClass: "Bank Bonds"`, `manualMode: "value"`, `derivedFromTransactions: true`. Wired em 3 sync points: load inicial, Refresh All e handleTransactionsChange (mesmo padrao do item 32). Holdings Cash e BRA Fixed Income intocados.
- **Item 36** (jun/2026 — PR #86, estendido em PR #87): Income de Bank Bonds em `src/Dividends.jsx`. **Base (PR #86):** accrual estimado 100% no frontend a partir de buy/sell + cupom/maturidade (`parseBondNotes`, `computeBankBondsAccrual`, replay por CUSIP, ACT/365, principal vigente, capado em min(hoje, maturidade), bucketing por mes). **Estendido (PR #87):** pagamentos reais de juros importados do Fidelity Account History entram no calculo — `computeBankBondsAccrual(transactions, bondIncome)` agora mescla pagamentos reais (bucketados no mes real) com o accrual estimado que preenche **apenas o gap apos o ultimo pagamento real** (sem double-count). Calibra `couponFreq` pela cadencia dos pagamentos reais (exposto em `freqByCusip`). Subtitulo do KPI reflete real/estimado/misto. KPIs Y/Y comparam so dividendos de acoes (intencional). Sem bump de cache.
  - **Bug fix — accrual gerava pagamentos "EST" fantasma (jul/2026 — commit d93cd87, merge b96922a, mergeado direto em main):** o accrual (renomeado `buildBondEvents`/`accrueByFreqAsEvents`) sempre fatiava em blocos de calendario mensal fixo (ignorando `freqByCusip` ja calibrado — bonds trimestrais/semestrais mostravam "EST" todo mes) e sempre emitia um evento para o periodo corrente incompleto, datado em "hoje - 1 dia" (data arbitraria de abertura da pagina, nao data real de pagamento). Tambem havia um boundary bug: `accrueFrom` nao avancava `+1 dia` apos um pagamento real, gerando um "coto" residual sobreposto ao pagamento recem-importado. Fix: accrual dimensionado por `freqByCusip[cusip]` -> `couponFreq` -> `"monthly"`; evento "EST" so emitido para periodos inteiramente decorridos; `accrueFrom = lastRealDate + 1 dia`. Efeito esperado: menos eventos "EST", totais de Income History/KPIs mais baixos para bonds nao-mensais.
- **Desktop responsivo** (jun/2026 — PR #85): Container principal expandido de `maxWidth: 640` para `maxWidth: 1200` em `src/App.jsx`. State `windowWidth` com lazy init e listener de `resize` com cleanup no unmount — padrao de responsividade sem CSS media queries. `DonutChart` agora aceita prop `size` (default 140) com raios e fontes derivados proporcionalmente; os dois donuts da secao de alocacao recebem `size` dinamico via IIFE com clamp `[140, 220]`. Mobile (<640px) identico ao anterior — zero regressao.
- **Item 35** (jun/2026 — commit d6dc43b): Suporte a splits e groupings (reverse splits) em `src/Transactions.jsx`. Componente `SplitModal` com inputs (ticker, data, fator N:M), preview de impacto por transação (qty→nova / price→novo / total invariante), e botão Apply que muta as transactions anteriores à data do split com audit trail (`splitAdjusted: true`, `originalQty`, `originalPrice`). Validações: não aceita CUSIP nem tesouro-*, data não futura, fator N≠M. Downstream automático via `persist()` → `onTransactionsChange` → `applyTxQty` → cache perf-history v12 invalidado. Reverse split detectado quando denominator > numerator.
- **Item 36 follow-up — Bond Projections** (jun/2026 — commit d6dc43b): Card colapsável "Bond Projections" adicionado como 5º card em `src/Dividends.jsx`. Função pura `buildBondProjections` usa `freqByCusip` (frequência calibrada pelos pagamentos reais do Fidelity) para projetar os próximos pagamentos de cada CUSIP com posição aberta, até 12 meses ou vencimento. Valor estimado = principal × couponPct/100 × intervalDays/365. Valores mascarados por `valuesHidden`. Fecha o follow-up do item 36 que estava pendente desde PR #87.
- **Tab Events — filtro net qty > 0** (jun/2026 — commit d6dc43b): `extractEligibleTickers` em `src/Events.jsx` passou a filtrar tickers com posição líquida ≤ 0 — sells totais ou posições zeradas não geram chamadas à API de eventos.
- **Tab Events — grupos cronológicos colapsáveis** (jun/2026): os headers de grupo ("Last 7 Days", "Last Month", "Today", etc.) viraram botões colapsáveis com `ChevronDown` rotativo (mesmo padrão dos cards de Dividends/Performance). "Last Month" começa colapsado por default; demais expandidos. Estado de sessão (não persiste). Apenas `src/Events.jsx`.
- **Detecção e aprovação de splits/groupings** (jun/2026 — commit 4e66bd9): Sistema que detecta splits/groupings ocorridos (Yahoo/Polygon) ainda não refletidos no histórico e sinaliza com ícone Bell + badge no header. Novo endpoint `api/split-detect.js` (busca splits históricos completos, cache global). `splitEvents` no blob de transactions (applied/dismissed). Helper exportado `applySplitToTransactions` (audit trail + splitDate). Painel de revisão com preview e Approve/Dismiss por split. Estende o item 35 (SplitModal manual) com detecção automática. Aprovar ajusta o histórico e registra a data do split; dispensar persiste para não re-sinalizar (dados Fidelity já vêm split-adjusted).
- **UI polish — Events groups, Bond Projections, split UX, Last Refreshed** (jun/2026 — PR #104): Cinco ajustes. **(1)** Tab Events: `groupLabel()` simplifica buckets passados para só "Last 7 Days" e "Last 30 Days" (remove buckets mensais tipo "May 2026"); ambos começam colapsados por default. **(2)** Card "Bond Projections" (`Dividends.jsx`) começa colapsado (`bondProjOpen` default `false`). **(3)** UX de aprovação de split: botões Approve/Dismiss desabilitados + spinner "Applying…/Dismissing…" enquanto a chamada está in-flight (`splitActionInFlight` = key do split). **(4)** Histórico de splits: seção colapsável "History" no painel de revisão mostrando `splitEvents` decididos (applied/dismissed) com ticker, ratio, data e timestamp; mais recentes primeiro. **(5)** Texto "Last Refreshed" movido para baixo da linha de botões de ação (alinhado à direita).
- **Split review movido para Tab Transactions + Bell vira painel de Alerts** (jun/2026 — PR #105): **(1)** Botão "Split" manual removido da toolbar de Transactions (auto-detecção via Bell cobre o caso; edição manual de tx cobre edge cases). `SplitModal` mantido no arquivo (exporta `applySplitToTransactions`) mas sem instância renderizada. **(2)** Card colapsável "Splits / Groupings" adicionado na Tab Transactions (abaixo da toolbar, acima do form de nova tx): seção Pending com preview/approve/dismiss + seção History colapsável. `TransactionsView` recebe props `pendingSplits`, `splitEvents`, `splitActionInFlight`, `onApproveSplit`, `onDismissSplit`. **(3)** Bell do header generalizado de popup só-de-splits para painel de Alerts multi-propósito: split pendente (linha clicável que navega para Transactions), dividendos pagos hoje (`/api/events` payout), earnings hoje (`/api/events` earnings), bond maturities em até 7 dias (derivado localmente das transactions). `refreshTodayAlerts()` chamado no startup junto com `refreshPendingSplits()`.
- **Alerts — log persistente, bundled por data, valor de dividendo, mark-as-read** (jun/2026 — PR #106): Painel de Alerts evoluído de "só hoje" para um log rolante persistido em `localStorage` (`alertLog`, cap 50, exibe últimos 10). Cada alerta ganha `id` estável (`tipo|ticker|data`), `sentDate` (data de detecção) e `read`. `refreshAlerts()` faz merge dedupe no log. **Bundling:** `groupAlertsByDate()` + `formatAlertDate()` agrupam os 10 últimos por data ("Today"/"Yesterday"/"Mon D, YYYY"). **Valor de dividendo:** alerta de payout calcula `qtyHeld x $/share` e exibe "Dividend paid: $X" + detalhe "N sh x $Y/sh"; respeita `valuesHidden`. **Read state:** botão check por alerta (`markAlertRead`) + botão "Mark all read" no header (`markAllAlertsRead`); lidos ficam dimmed. Badge do Bell = `pendingSplits + alertas nao lidos`.
- **Validacao de ticker no TransactionForm** (jun/2026 — PR #107): Funcoes module-level `shouldSkipValidation` e `validateTickerViaAPI` adicionadas em `src/Transactions.jsx`. `TransactionForm` recebeu prop `auth` e estados `tickerValidating`/`tickerError`. Trigger no onBlur do campo Ticker: chama `GET /api/price?ticker=X` e exibe erro inline se a API retornar negativo explicito. Falha de rede e silenciosa (nao bloqueia). Tickers que pulam validacao: `tesouro-*`, classe `BRA Fixed Income`, classe `Bank Bonds`, CUSIPs.
- **Onboarding Holdings — texto corrigido** (jun/2026 — PR #107): Mensagem de empty state "No positions yet. Add your first ticker above." atualizada para "No positions yet. Add transactions in the Transactions tab to get started." em `src/App.jsx`. Remove referencia ao form de adicionar ticker que foi removido no item 33 (PR #84).
- **Bond Projections — sub-header com frequencia, coupon e maturidade** (jun/2026 — PR #107): Sub-header de cada bond no card "Bond Projections" (`src/Dividends.jsx`) agora renderiza a frequencia calibrada + coupon rate + maturity date (ex: "Monthly · 5.45% · Matures Mar 2027"). `freqByCusip` ja era computado e exposto por `buildBondEvents`; apenas a renderizacao foi reorganizada para exibir esses metadados na UI.
- **Bug fix — alertas marcados como lidos reaparecem apos novo login (jun/2026 — PR #109):** A chave `"alertLog"` no localStorage nao era scoped por usuario, causando contaminacao cruzada quando dois usuarios alternavam login no mesmo browser. Fix em `src/App.jsx`: nova funcao module-level `alertLogKey(auth)` gera chave `alertLog:g:<email>` (Google) ou `alertLog:p:<senha[:8]>` (password auth); `useState` lazy-init e `useEffect` de persistencia usam a chave scoped; migracao one-time da chave legada `"alertLog"` preserva historico existente no primeiro login apos o deploy.
- **Itens 25/26/27 + rollover + historico Redis — Tab Contributions completa (jun/2026 — PR #112):** Duas partes entregues em `src/AporteQuinzenal.jsx` + novo endpoint `api/contributions-history.js`. **Chunk A (frontend puro):** logica de rollover de quinzena — deficit nao aportado na 1a quinzena rola para a meta da 2a: `half2Target = halfPlanned + max(0, halfPlanned - half1Auto)`; excesso na 1a reduz a meta da 2a; total do mes (`planTotal`) nunca muda. UI: sufixo "+R$X rollover" na 2a quinzena; linha totalizadora "Month total / Invested / Remaining for month" com estado "Month Done" verde. **Chunk B (Redis):** novo endpoint `api/contributions-history.js` (GET + PUT); chave Redis `portfolio:<storageKey>:contributions-history` mapeando `"YYYY-MM"` para snapshot `{ monthlyFixed, dividends, dellSale, extras, planTotal, invested, savedAt }`; auto-snapshot idempotente do mes corrente em cada load; PUT e read-modify-write que preserva meses passados; tabela "Contribution Capacity History" com scroll horizontal para iPhone, valores mascarados por `valuesHidden`, ordenada mais-recente-primeiro. Limitacao: meses anteriores ao primeiro uso nao tem historico — acumula organicamente daqui para frente.
- **Design polish — Holdings e Cash como cards de destaque (jun/2026 — PR #113):** Modificacao cirurgica no componente `SectionLabel` em `src/App.jsx`. Adicionado elemento de acento dourado (gradiente `linear-gradient(to right, ${T.gold}, transparent)`, `height: 2px`, posicionado absolutamente no topo do wrapper) como primeiro filho do componente. `fontSize` do label aumentado de `9` para `12`. `position: "relative"` e `overflow: "hidden"` adicionados ao `baseStyle` para conter o elemento absoluto. Afeta os dois usos do componente na tab Holdings: headers "Your Holdings" e "Cash & Fixed Income". Visual consistente com os cards de destaque da tab Contributions.
- **UX cleanup — Remover botao de delete de auto holdings (jun/2026 — PR #114):** Icone Trash2 removido do componente `HoldingRow` (`type: "auto"`, derivados de Transactions) em `src/App.jsx`. Holdings manuais puros (`type: "manual"`, sem `derivedFromTransactions`) continuam com botao de delete disponivel. Holdings `bank-bonds-aggregate` e outros com `isBankBonds` guard em `ManualHoldingRow` ja eram protegidos desde PR #93 — nao mudaram. Para remover um auto holding, o usuario deve deletar as transacoes correspondentes na tab Transactions. Funcao `removeHolding` mantida (usada por `ManualHoldingRow`).
- **Bulk Import de Asset Allocation via CSV (jun/2026 — PR #115):** Botao "Import CSV" na secao "Your Holdings" da tab Holdings abre modal de 2 steps (Upload -> Preview). CSV de 2 colunas: `ticker, target %`. Preview classifica cada linha em 3 actions: "Update target" (ticker ja existe em holdings), "From transactions" (ticker existe em Transactions mas nao em Holdings, cria holding auto-style com asset class das transactions) ou "New holding" (ticker totalmente novo, exige dropdown de asset class + validacao async via `GET /api/price`). Holdings novos criados com `qty: 0` e `fromCSVImport: true` — compativeis com `applyTxQty` e Refresh All. Bank Bonds nao criavel via CSV (aviso inline). `HoldingRow` exibe botao delete e dropdown de asset class para holdings `fromCSVImport: true`; ambos desaparecem quando o ticker ganhar transactions. ~430 linhas adicionadas em `src/App.jsx`.
- **Bug fix — alertas re-aparecem como nao lidos entre sessoes (jun/2026 — PR pendente de merge):** `auth` adicionado ao dependency array do `useEffect` que persiste `alertLog` no localStorage em `src/App.jsx`. Sem isso, se `auth` mudasse apos o mount (ex: refresh de token Google), a closure capturada usava a chave de armazenamento da sessao anterior, causando divergencia entre a chave de leitura e a de escrita do alertLog.
- **Enhancement — estimated vs reported EPS na tab Events (jun/2026 — PR pendente de merge):** Funcao `EventDetail` para tipo `earnings` em `src/Events.jsx` exibe, quando `epsActual` esta disponivel: "Est: $X -> Reported: $Y" com indicador beat (verde) / miss (vermelho) / in-line (neutro) em `<span>` colorido. Eventos futuros (so `epsEstimate`) continuam inalterados.
- **Bug fix — Fidelity parser descartava transacoes ex-div (jun/2026 — PR #116):** `startsWith("YOU BOUGHT")` e `startsWith("YOU SOLD")` trocados por `includes(...)` em `src/Transactions.jsx`. Fidelity emite acoes recebidas como ex-dividend com Action "YOU BOUGHT EX-DIV DATE..." — o startsWith falhava silenciosamente e a transacao era descartada no import.
- **Bug fix — double-count em "Dividends (last month)" na tab Contributions (jun/2026 — PR #116):** Filtro de `bondTotal` em `src/AporteQuinzenal.jsx` corrigido de `e.kind === "interest" || !e.kind` para `e.kind === "interest"`. A clausula `|| !e.kind` incluia entradas sem o campo `kind` no total de bonds, causando double-count com `apiTotal` (dividendos de acoes do endpoint). O campo "Dividends (last month)" volta ao valor correto.
- **Bug fixes — Fidelity CSV parser (jun/2026 — PR pendente de merge):** Cinco correcoes em `src/Transactions.jsx` e `api/dividends.js`: **(1)** `includes("YOU BOUGHT")` e `includes("YOU SOLD")` (ja presente desde PR #116) estendido para o guard do bloco de dividendo — ETF names com "DIVIDEND" (ex: NOBL, SCHD) nao sao capturados erroneamente como income. **(2)** Guard duplo no bloco de dividendo: `!upper.includes("YOU BOUGHT") && !upper.includes("YOU SOLD")` evita que linhas "YOU BOUGHT ex-div" de ETFs chamados "DIVIDEND ARISTOCRATS" caiam no path de dividendo. **(3)** Linhas com action comecando por "DISTRIBUTION" adicionadas a lista `distributionEvents` para purga automatica em re-import (self-healing de entradas mal capturadas). **(4)** Linhas "INTEREST EARNED CASH" (juros de sweep/core-cash, symbol de 9 digitos numerico como `315994103`) excluidas do capture de bond income e adicionadas ao purge list — nao sao instrumentos de renda fixa. **(5)** `api/dividends.js` corrigido para dedup por `(ticker, month)` em vez de por ticker inteiro — import parcial de um mes nao mais silencia o historico Yahoo daquele ticker em outros meses. Cache bumped `v6 -> v7`.
- **Bond Income Audit Panel (jun/2026 — PR pendente de merge):** Painel colapsavel "Import History — Bond Income" adicionado ao rodape da Tab Transactions (`src/Transactions.jsx`). Exibe todos os entries de `bondIncome` armazenados no Redis (date, ticker, kind, source, amount), ordenados mais-recente-primeiro. Cada entry tem dois passos de delete (trash -> confirmar) que persiste imediatamente no Redis via PUT de transactions. Permite auditar e limpar entries stale ou mal capturados sem acesso direto ao Redis.
- **Dividends Monthly Map (jul/2026 — commit `bfe852c`, merge direto em main):** Novo card colapsavel na tab Dividends (`src/Dividends.jsx`), heatmap ano x mes mostrando total de dividendos recebidos por mes (soma de `totalReceived` de `allEvents`, incluindo stock dividends, bond interest e foreign tax negativo — mesmo criterio dos KPIs existentes). Escala de cor single-hue dourada (`heatColorAmount`, baseada em `T.gold`), proporcional ao valor do mes. Coluna "Total" por ano = soma simples dos 12 meses (nao TWR/compounding). Meses sem eventos: celula transparente, sem "0". Anos em ordem decrescente. Card fechado por default (`mapOpen`). Posicionado logo apos "Income History" e antes de "Position Dividends". Sem mudanca de API/backend — 100% derivado de dados ja carregados no client.
- **Item 44 — Date Filter Redesign (Transactions) (jun/2026 — branch claude/date-filter-redesign-txezl2, commit f517df2):** Substituicao dos inputs manuais `From`/`To` de date range no `HeaderPopover` da coluna Date em `src/Transactions.jsx` por seletor de ano+mes derivado dos dados reais. Anos exibidos em ordem decrescente com botao `+/-` que expande/colapsa os meses com transacoes. Click num ano seleciona o range completo `YYYY-01-01` a `YYYY-12-31`; click num mes seleciona `YYYY-MM-01` a `YYYY-MM-lastDay`. Single-select com highlight dourado (`T.gold`) e botao "Clear". Estado `expandedYears` (Set) local ao `HeaderPopover`; `dateOptions` (Map) computado via `useMemo` no `TransactionTable`. Logica de filtragem downstream inalterada — apenas a UI do seletor mudou.
- **Auto-hide de holdings zerados sem target allocation (jun/2026 — PR #117):** Holdings com qty/value zero e sem `target > 0` sao ocultados visualmente via `filteredHoldings` useMemo em `src/App.jsx` — nao sao deletados do Redis. Helper module-level `isZeroHolding(h)` cobre os 3 modos: manual qty_price (`qty === 0`), manual value (`manualValue === 0`) e auto (`qty === 0`). Holdings com `target > 0` permanecem visiveis mesmo zerados (placeholders intencionais de rebalance). Denominador do `SectionLabel of={}` atualizado para excluir os mesmos holdings, mantendo contagem consistente.
- **Bug fix — extra label do Monthly Plan (Contributions) some ao reabrir o app (jul/2026 — PR #118):** Em `src/AporteQuinzenal.jsx`, `config.extras` dependia exclusivamente de `localStorage["aporteConfig"]`, que podia ser perdido (ex: Safari iOS limpando site storage), fazendo a extra label desaparecer no dia seguinte. Fix: `useMemo` `currentMonthSnapshot` le `capacityHistory[currentMonthKey()]`; novo `useEffect` de restore one-time (guard via ref `seededExtras`, mesmo padrao de `seededFixed`) repopula `extras` a partir do snapshot Redis do mes corrente quando o array local esta vazio e o snapshot tem dados — nunca sobrescreve edicoes feitas pelo usuario apos o load. Transforma o auto-snapshot do Redis (existente desde PR #112) de write-only em tambem uma rede de seguranca de leitura.
- **Redesign visual do bloco de filtros — grafico Dividend Evolution (jul/2026 — PR #119):** Ajuste de design em `src/Dividends.jsx`, sem mudanca de logica/dados. **(1)** Seletor de granularidade do grafico (era botoes "Month"/"Quarter"/"Half"/"Year") agora mostra labels curtos "M"/"Q"/"H"/"Y" (texto completo continua acessivel via tooltip `title`). **(2)** Seletor movido para ficar colado imediatamente acima do grafico (antes ficava no topo do bloco de filtros). **(3)** Ordem das secoes de filtro reorganizada: Year filter -> Ticker/Asset Class filter (selects) -> seletor M/Q/H/Y. **(4)** Bloco de texto descritivo ("US dividends only...") e o espacamento abaixo do grafico removidos. Build (`npm run build`) verde.
- **Filtros Year/Ticker/Asset Class viram multi-select via popover custom (jul/2026 — PR #121-#124):** Continuacao direta do redesign do PR #119, mesmo arquivo `src/Dividends.jsx`. **PR #121:** filtro Year virou multi-select usando `<select multiple>` nativo. **PR #122:** opcao "All years" removida da lista; adicionado botao "Clear" dedicado para resetar o filtro. **PR #123:** botao Clear movido para um "x" sobreposto no canto superior direito de cada seletor (Year, Ticker, Asset Class). **PR #124 (mudanca final e mais significativa):** `<select multiple>` nativo abre um picker OS-controlado no iOS/WebKit que nao pode ser customizado — impossivel colocar um botao Clear dentro do header nativo. Os 3 selects nativos foram substituidos por um componente proprio `FilterMultiSelect` (mesmo arquivo): chip-trigger que abre um popover proprio (nao nativo) com checkboxes e um header com "Clear" + "x" lado a lado. Ticker e Asset Class tambem viraram multi-select (antes single-select via `<select>` com opcao "All ..."). Labels dos triggers mostram "Years"/"Tickers"/"Classes" quando nada esta selecionado. Resultado visivel: tres chips (Years, Tickers, Classes) lado a lado acima do grafico, cada um com popover custom de checkboxes; seletor M/Q/H/Y (PR #119) permanece colado ao grafico logo abaixo. Build (`npm run build`) verde em todos os 4 PRs.
- **Toggle ALL / NON-DUP / DUP no preview de import de transacoes (jul/2026 — PR #127):** Segmented control adicionado ao `ImportModal` em `src/Transactions.jsx` para filtrar visualmente a tabela de preview durante import Fidelity/CSV. State `dupFilter` ("all" | "non-dup" | "dup", default "all"); so aparece quando `duplicateCount > 0`. Reutiliza a deteccao de duplicata existente (`dupKey`, `dupKeySet`, `annotateDuplicates`, `duplicateCount` — item 34) sem nova logica de dedup. Filtro e puramente visual (early-return no `.map` da tabela), nao altera indices usados por `checkedRows`/`editingIdx`; `handleConfirm` continua importando exatamente o que esta marcado nos checkboxes, independente do filtro selecionado. Unico arquivo alterado.
- **Bug fix — sincronizacao cross-device do estado "lido" dos Alerts / Bell (jul/2026 — PR #128, merged em main):** O estado "lido" das notificacoes do sino vivia so em `localStorage` — marcar dividendos/eventos como lidos no PC nao refletia no iPhone ate 1h+ depois (nunca era enviado ao servidor). Endpoint estendido: `api/contributions-history.js` ganhou uma segunda rota dispatched via query param `?resource=alerts-read` (funcao `handleAlertsRead`) — **nao** um arquivo `api/alerts-read.js` novo (foi criado e depois deletado; ver Decisoes Tecnicas sobre o limite de 12 Serverless Functions do Vercel Hobby). Chave Redis `portfolio:email:<hash>:alerts-read` (ou `:pwd:`). GET retorna `{ exists, readIds, savedAt }`; PUT `{ add: string[] }` faz read-modify-write com uniao, capado em 200 ids. `src/App.jsx` ganhou `fetchAlertsReadFromServer`/`saveAlertsReadToServer` (fire-and-forget); no load inicial sobrescreve `read: true` nos alertas ja carregados do localStorage a partir do `readIds` do servidor; `mergeAlerts` usa esse set para decidir o `read` inicial de alertas novos; `markAlertRead`/`markAllAlertsRead` mantem update local otimista + disparam PUT em background.
- **Bug fix — timezone incorreto no calculo de "hoje" (jul/2026 — PR #128, mesma branch, merged em main):** `new Date().toISOString().slice(0,10)` usa UTC; num timezone negativo (usuario reportou o bug estando em Austin, TX — US Central, UTC-5/-6, nao no Brasil) domingo a noite local ja e segunda em UTC, entao um dividendo aparecia como "pago hoje" incorretamente. Novo helper `localTodayISO(d)` le o offset de timezone do proprio browser/device (nao hardcoded a nenhum fuso), duplicado em `src/App.jsx` (painel de Alerts) e `src/Events.jsx` (agrupamento Today/Last 7 Days + `isPast` da Tab Events) — sem shared module entre arquivos `src/`, padrao ja estabelecido no projeto. Build (`npm run build`) validado duas vezes: antes e depois da consolidacao do endpoint.
- **Filtro multi-select de ticker nas tabelas Position Performance e Position Dividends (jul/2026 — PR #126, draft aguardando merge):** Icone `Filter` sempre visivel no `<th>` Ticker (`e.stopPropagation()` para nao disparar o sort existente), abre popover custom `TickerFilterPopover` com checkboxes — mesma familia de padrao do `DivHistPopover` (Dividend History, item 30/PR #71) e do `FilterMultiSelect` (Income History, PR #121-124). Implementado em `PositionPerformanceTable` (`src/Performance.jsx`) e `PositionDividendsTable` (`src/Dividends.jsx`); componente duplicado localmente em cada arquivo (convencao do projeto), state Set-based independente e efemero por tabela (nao persiste, nao compartilha entre os dois cards). `allTickers` do popover sempre derivado das rows nao filtradas; filtro aplicado em sort/display rows, totais e agrupamento por classe. Round 2 corrigiu bug: `<thead>` inteiro (incluindo o icone-ancora) sumia quando o filtro zerava os resultados, travando o usuario — fix manteve o `<thead>` sempre montado e moveu o empty-state para uma linha dentro do `<tbody>`. Sem mudanca em `api/`, sem bump de cache.
- **Fix pay-date de dividendos para ADRs sem cobertura Polygon — caso TSM (jul/2026 — merge `d0054d4`):** TSM (ADR NYSE, emissor estrangeiro 20-F) pagou dividendo em 09/jul/2026 mas o app mostrava 11/jun/2026 (ex-date), pois a Polygon nao tem cobertura de pay-date para esse ticker e `api/dividends.js` caia num fallback silencioso pra ex-date. Fix: novo lookup lazy-por-ticker via Finnhub `/stock/dividend` (cache Redis dedicado `dividends:paydates:finnhub:v1:{TICKER}`, TTL 7 dias) disparado quando o Polygon nao resolve; quando nem Polygon nem Finnhub resolvem, o evento ganha `payDateUncertain: true` (ex-date continua sendo usada como `date`, mas fica marcada como incerta) em vez de sumir ou aparentar ser uma data confirmada. `src/Dividends.jsx` ganhou badge visual "EX-DATE" (ambar) na tabela Dividend History para esses casos. `CACHE_VERSION` `v7 → v8`. Pendencia: cobertura real da Finnhub para ADRs 20-F nao pode ser validada no sandbox de dev (finnhub.io bloqueado por politica de rede) — validar pos-deploy via `meta.payDatesResolvedViaFinnhub` (ver CONTEXT.md, secao Estado Externo).
- **$/share e qty derivados para eventos de dividendo importados da Fidelity (jul/2026 — merge `e990942`):** Antes sempre `"—"` (so o total exato era exibido). `api/dividends.js` agora deriva `qty` via `qtyAtDate(transactions, ticker, date)` (mesmo helper dos eventos reconstruidos via API) e `amountPerShare = total / qty`, aproximacao valida no caso comum (qty raramente muda entre ex-date e pay-date). `CACHE_VERSION` `v8 → v9`.
- **Foreign tax withheld em dividendos de ADR — caso TSM/VALE (jul/2026 — merge `1cde08f`):** Fidelity reporta o imposto retido de ADRs (TSM, VALE) como uma linha `FOREIGN TAX PAID` separada da linha de dividendo, mesmo dia — o parser (`parseFidelityCSV`, `src/Transactions.jsx`) descartava essa linha silenciosamente (`else continue`). Fix: nova captura como `kind: "tax"` no `bondIncome`; `api/dividends.js` retorna um array `foreignTax` separado de `events` (dividendo permanece bruto, sem double-count no proprio endpoint). `src/Dividends.jsx` mergeia `foreignTax` na tabela Dividend History (linha vermelha com badge "TAX", `$/Share`/`Qty` = "—", total negativo) e exibe um resumo Gross/Foreign Tax/Net acima da tabela quando ha ao menos uma linha de imposto visivel — o `TOTAL` row ja soma tudo automaticamente. `CACHE_VERSION` `v9 → v10`. `BondIncomeAudit` (Transactions) e o toast de import ganharam contagem/total separados para "tax".
- **Foreign tax netado em todos os agregados de dividendo, nao so na tabela de auditoria (jul/2026 — merge `11634d3`):** A primeira versao (item acima) so mostrava o tax na tabela Dividend History, sob a suposicao errada de que o dividendo ja vinha liquido. Conferido contra um export real da Fidelity — o valor e **bruto** (TSM $8.45 bate com a taxa oficial $/ADS × qty). Fix: `foreignTax` agora e mergeado direto no `allEvents` de `src/Dividends.jsx` (abate de KPIs, Income History chart, Position Dividends, Y/Y) e `src/Performance.jsx` (Total Return, Div TTM/Total/YoC de Position Performance); `src/AporteQuinzenal.jsx` (KPI "dividends last month") tambem passou a netear.
- **Alerts panel usa valor real da Fidelity em vez de estimativa Yahoo/Finnhub (jul/2026 — merge `2e386f2`):** Ultimo lugar do app com calculo de dividendo desalinhado — o badge "Dividend paid: $X" (`refreshAlerts` em `src/App.jsx`) usava `api/events.js`, pipeline separado que nunca consultava `bondIncome`. Caso real: badge da AMT mostrava $91.29 (estimativa Yahoo desatualizada, $1.79/sh) enquanto o import real da Fidelity — e a Tab Dividends — tinham $71.60. Fix: `fetchTransactionsForSync(auth, true)` agora tambem retorna `bondIncome`; `refreshAlerts(txs, bondIncome)` prefere o valor exato do import Fidelity de hoje (dividendo `kind:"dividend"` liquido de eventual `kind:"tax"` do mesmo dia/ticker) sobre a estimativa ao vivo, quando disponivel. `$/share` do detalhe "N sh × $Y/sh" re-derivado do valor real pra ficar consistente.
- **`mergeAlerts` atualiza alertas ja gravados em vez de so adicionar novos (jul/2026 — merge `e320299`):** o fix acima nao se refletia sozinho — o alerta da AMT ja estava gravado no `alertLog` (localStorage) com o valor antigo antes do fix, e `mergeAlerts` fazia dedup so por `id`, nunca atualizando uma entrada existente. Fix: ao reprocessar, um alerta detectado com `id` ja conhecido mas payload diferente (`message`/`detail`/`total`/`amount`) tem o conteudo atualizado in-place, preservando `sentDate` e `read` state.
- **Tab Events: valor $ de dividendo pago/estimado + beat/miss como badge (jul/2026 — merge `53abcf8`):** cards `ex_dividend`/`payout` mostravam so o $/share, nunca o valor total. Agora mostram tambem o valor em $: exato via import Fidelity (liquido de foreign tax, mesma fonte do badge de Alerts) quando disponivel, senao estimado via `qty atual × $/share`. Label "Dividend paid: $X" (passado/hoje) ou "Estimated dividend to be paid: $X" (futuro); estimativas sem confirmacao real ganham sufixo "(est.)". `ex_dividend` so mostra o valor quando nao ha `payDate` conhecido (evita duplicar com o card `payout`). Earnings beat/miss (ja existente desde jun/2026) virou um pill colorido em vez de texto inline, mesmo peso visual do `TypeBadge`. `valuesHidden` (recebido mas nunca usado nesse arquivo) agora mascara os novos valores em $.
- **Fix — dividendos contabilizados um dia adiantado por timezone UTC do servidor (jul/2026 — merge `76c6131`):** `api/dividends.js` usava `new Date().toISOString()` (UTC do Vercel) pra decidir se um dividendo ja foi pago. Pra timezones negativos (US Central, Brasil), UTC vira o dia seguinte horas antes da meia-noite local — caso real: as 22h50 CDT de 14/jul, a Tab Dividends inteira ja contava um dividendo de 15/jul como recebido. Fix: endpoint aceita `body.todayISO` opcional do cliente (fallback pro UTC se ausente); `Dividends.jsx`, `Performance.jsx`, `AporteQuinzenal.jsx` passaram a mandar `localTodayISO()` (mesmo helper de `App.jsx`/`Events.jsx`). `CACHE_VERSION` `v10 → v11`. Mesmo commit tambem resolveu a raiz do bug do badge AMT preso em $91.29 (roadmap acima): `refreshAlerts` so reprocessava alertas de dividendo com data === hoje, entao um alerta gravado no dia do pagamento nunca era revisitado em dias seguintes mesmo apos importar o extrato real — ganhou um segundo passo que reconcilia **todos** os alertas de dividendo (qualquer data) contra o `bondIncome` completo a cada carregamento.
- **Sort de colunas clicavel em "Dividends Monthly Y/Y" (jul/2026 — commit `0ecde97`):** Refinamento pontual do item 29 (Redesign "Dividends Monthly Y/Y"). Headers da tabela `YearVsYearTable` (`src/Dividends.jsx`) tornam-se clicaveis, com toggle asc/desc a cada clique e indicador visual (`↕` inativo, `↑`/`↓` ativo) — reaproveita o mesmo padrao ja existente em `PositionDividendsTable` no mesmo arquivo. Novo state `sortCol`/`sortDir` (default `sortCol: "cy"`, `sortDir: "desc"` — antes o sort era fixo por `cy+py` combinado, sem toggle). Colunas ordenaveis: nome (Ticker/Class, `localeCompare`), PY, CY, Delta $ (`cy-py`), Delta % (`py>0 ? (cy/py-1)*100 : null`, nulls sempre no fim). Aplicado nos 3 niveis: `classGroups` (By Class), tickers dentro de cada grupo colapsavel, e `tickerRows` no modo flat (By Ticker). Linha TOTAL permanece fixa no topo, fora do sort. Mudanca client-side apenas, sem alteracao de API/cache. Unico arquivo alterado: `src/Dividends.jsx` (+81/-12).
- **Filtros (Asset Class + Ticker) e toggle independente "Show Total Return" no grafico Performance (jul/2026 — commit `a60bb9f`):** Dois filtros multi-select (chip com popover, mesma familia de `FilterMultiSelect`/`TickerFilterPopover` ja estabelecida no app) adicionados acima do card "Portfolio Performance & Net Worth" em `src/Performance.jsx`. **Asset Class filter:** nova constante local `PERF_ELIGIBLE_CLASSES` (8 classes — Stocks, BRA Stocks, Alternative, Real Estate, Bonds, Bank Bonds, BRA Fixed Income, Unallocated USD — duplicada de `INCLUDED_CLASSES` de `api/perf-history.js`) intersectada com as classes realmente presentes nas transactions do usuario. **Ticker filter:** opcoes dependem do Asset Class filter ja aplicado; trocar o Asset Class filter poda automaticamente o Ticker filter, removendo tickers que ficariam fora das classes selecionadas (evita combinacao permanentemente vazia). **Toggle "Show Total Return":** desacopla a linha/KPI de Total Return do toggle "Compare vs S&P 500" — antes um unico toggle controlava 3 linhas (Portfolio/Total Return/SPY) simultaneamente; agora cada linha e independente. Estado default (ambos off) reproduz exatamente o comportamento anterior, sem regressao. Filtro 100% client-side, aplicado no array de `transactions`/`divEvents` antes do `POST /api/perf-history` — nenhuma mudanca em `api/perf-history.js`, `api/dividends.js` ou `App.jsx`; o cache Redis v12 (hash de transactions) ja produz cache correto para o subconjunto filtrado enviado pelo cliente, sem bump de versao. KPI "Net Worth" vira "Net Worth (filtered)" com valor de `positionRows` filtrados quando algum filtro esta ativo. `useEffect` de carregamento dividido em dois: dados brutos `[auth]` e POST filtrado `[auth, filteredTransactions]`, guardado por estado explicito `transactionsLoaded` (nao `transactions.length`, que travaria contas com zero transacoes em loading infinito — bug pego na auditoria). Refetch so ao fechar o popover do filtro, nao a cada clique de checkbox. Botao "Clear filters" sempre acessivel mesmo com resultado filtrado vazio (outro bug pego na auditoria: a barra de filtros nao pode depender de `rawData.length > 0` pra renderizar). Unico arquivo alterado: `src/Performance.jsx` (+360/-89 entre commit de feature e commit de bugfix pos-auditoria). Rodada 1 de auditoria reprovou por esses 2 bugs; rodada 2 aprovou. Merge direto em `main` via `git merge --no-ff`, sem PR.
- **Redesign — botao "Import Allocation Targets" movido para fora do card Holdings (jul/2026 — commit c8c891f, merged em main):** Botao antes acoplado ao header do `SectionLabel` "Holdings" (so visivel quando `filteredHoldings.length > 0`) agora vive em um `<section>` proprio e independente em `src/App.jsx`, posicionado acima do card "Add Manual Asset" e sempre visivel (inclusive com a conta de holdings vazia). Estilo replica fielmente o card "Add Manual Asset" (`background: T.card`, `border: 1px solid T.borderSoft`, `borderRadius: 4`, `padding: 14`, `marginTop: 18`); botao usa tipografia mono dourada uppercase com icone `Upload` (mesmo padrao visual do icone `Plus` de "Add Manual Asset"). `SectionLabel` "Holdings" simplificado (removido o wrapper flex que antes o acoplava ao botao). Nenhuma logica de import CSV alterada — mesma feature de bulk import de asset allocation (PR #115), puro reposicionamento visual + estilo.
- **Composition Evolution — novo card na tab Performance (jul/2026 — commit `15df95d`, merge `1fbb4c3`, merged em main; ajustes de UX jul/2026 direto em `main`):** Card colapsavel "Composition Evolution" em `src/Performance.jsx`, posicionado **depois** de "Position Performance" (ordem final: Portfolio Performance & Net Worth → Position Performance → Composition Evolution — reordenado no ajuste). Mostra a evolucao historica da composicao do portfolio por asset class como stacked area chart normalizado a 100% (`recharts`, `stackOffset="expand"`), com filtros de periodo (1Y/2Y/5Y/All) proprios. **Filtro de Asset Class e de Ticker** (ajuste): dropdown no mesmo formato (`FilterChip`) do filtro "Asset Class"/"Ticker" do card "Portfolio Performance & Net Worth", mas com estado independente (`compAssetClassFilter`/`compTickerFilter`) — substituiu os pills de toggle client-side da versao original, que so tinha filtro de classe (sem ticker) e nao restringia os dados na origem, so a renderizacao. **Eixo X** (ajuste): agora usa a mesma logica de ticks por fronteira de calendario do grafico de TWR principal (`computeXAxis`, generalizada para os periodos "2Y"/"All") em vez das datas reais de transacao — a versao original deixava os rotulos com espacamento visualmente desigual. **Rodape** (ajuste): texto simplificado para so "Excludes BRA Fixed Income". `BRA Fixed Income` excluida (sem preco de mercado). Bank Bonds: accrual antes hardcoded em `Date.now()` dentro de `positionRows` foi extraido para o helper generico `computeBankBondsValueAt(transactions, asOfISO)`, reusado nos dois lugares (agora recebendo `compFilteredTransactions`). Backend (inalterado no ajuste): funcao pura `computeCompositionSeries` em `api/perf-history.js`; POST usa o campo opcional `allTransactions` (agora recebendo o subconjunto ja filtrado pelo card, nao mais o portfolio bruto) para computar a composicao sem duplicar a chamada de rede — TWR continua usando so `transactions` (respeitando o filtro global da pagina). Cache v13 inalterado (nenhuma mudanca de shape no ajuste). `npm run build` verde, `node test/perf-history.test.mjs` 11/11 sem regressao apos o ajuste. Pendente (nao-bloqueante, ver abaixo): testes formais para `computeCompositionSeries`/`computeBankBondsValueAt`; limpeza de campos mortos (`pos.lastBuyDate`/`pos.lastBuyNotes`) em `positionRows`.
- **Composition Evolution — fix de performance + reorganizacao visual (jul/2026, direto em `main`):** Bugfix critico: aplicar o filtro de Asset Class/Ticker do card levava 1-2s pra atualizar o grafico, porque cada toggle refetchava `/api/perf-history` (fetch ao vivo de candles no Twelve Data/brapi quando o subconjunto de tickers filtrado nao estava em cache). Fix: `computeCompositionSeries` (backend, `api/perf-history.js`) passou a retornar granularidade **por ticker** (`{ dates, tickerValues, tickerClass }`) em vez de ja agregado por classe — sempre para o portfolio inteiro. O frontend busca essa serie **uma unica vez** por carregamento de pagina (novo `compositionRaw` state, Effect C agora depende so de `transactions`) e aplica o filtro de Asset Class/Ticker **inteiramente client-side** (`filteredComposition`, `useMemo` que soma `tickerValues` dos tickers que passam no filtro) — toggle agora e instantaneo, zero round-trip de rede. **Cache bump v13→v14** (mudanca de shape do campo `composition`). Ajustes visuais: chips de filtro (Asset Class/Ticker) movidos para **acima** do filtro de periodo no card; filtro de periodo (1Y/2Y/5Y/All) redesenhado com preenchimento solido dourado quando ativo (mesmo estilo do seletor de periodo do card "Income History" em `Dividends.jsx`) — o filtro de periodo do card "Portfolio Performance & Net Worth" (`PERIODS`) recebeu o mesmo tratamento visual (era azul translucido). `npm run build` verde, `node test/perf-history.test.mjs` 11/11 sem regressao; sanity-check ad-hoc rodado contra `computeCompositionSeries` confirmando a nova forma de retorno.
- **Composition Evolution — toggle Class/Ticker + toggle Area/River (jul/2026, direto em `main`):** Dois novos `SegmentedToggle` (componente 2-opcoes reutilizavel) na mesma linha do filtro de periodo, alinhados a direita. **Class/Ticker:** alterna `viewBy` entre visualizar o stack por asset class (default) ou por ticker individual — modo Ticker usa `mergedTickerComposition` (nova serie client-side, mesma forma `{ dates, classValues }` mas com chaves de ticker, reaproveitando a granularidade por ticker que ja vinha do backend desde o fix de performance anterior); ordenado por contribuicao total decrescente; cores via novo array ciclico `TICKER_PALETTE` (nao ha mapa fixo de cor por ticker como existe por classe); Bank Bonds no modo Ticker usa `computeBankBondsValueAt(...).byTicker` (ja existia, usado pela primeira vez). **Area/River:** alterna `chartStyle` entre `"area"` (default, `stackOffset="expand"`, eixo Y 0-100%) e `"river"` (streamgraph, `stackOffset="wiggle"`, eixo Y e grid horizontal ocultos — referencia visual: screenshot de theme river anexado pelo usuario no chat). `npm run build` verde, `node test/perf-history.test.mjs` 11/11 sem regressao (mudanca 100% frontend, backend intocado).
- **Scroll Hint — indicador visual de swipe horizontal em tabelas (jul/2026, commit `cfb5d3b`, merge `0e4f9b0`, merged em main):** Componente `ScrollHintTable` (duplicado module-level em 5 arquivos — `Transactions.jsx`, `Performance.jsx`, `Dividends.jsx`, `AporteQuinzenal.jsx`, `App.jsx` — mesma convencao de duplicar componentes pequenos por arquivo ja usada por `TickerFilterPopover`/`FilterMultiSelect`) aplicado em 11 tabelas com scroll horizontal: tabela principal de Transactions, Position Performance, Position Dividends, Contribution Capacity History, Dividends Monthly Y/Y, Dividend History, Bond Income Audit Panel, Fidelity Import staged review, Splits/Groupings preview, Bond Projections payments, e o preview do modal "Import Allocation Targets" em `App.jsx`. Dois elementos: **(1)** fade de borda persistente (esquerda/direita), sempre visivel via `ResizeObserver` quando `scrollWidth > clientWidth`, independente de qualquer flag — cor ajustavel por `fadeBg` e offset esquerdo (`leftFadeOffset`, usa `TICKER_COL_WIDTH=92px`) para nao ficar atras de colunas `Ticker` sticky. **(2)** pill "Swipe" animado one-shot (2 icones `ChevronRight`, oscilacao `translateX` via `setInterval` + `transition` inline, sem `@keyframes` novo), persistido globalmente via `localStorage["scrollHintSeen"]` — uma vez dispensado em qualquer tabela, nao reaparece em nenhuma. Respeita `prefers-reduced-motion`. Fora de escopo por pedido explicito do usuario: graficos (recharts), tab bar do view switcher, pills de filtro de Events, `SplitModal` (componente morto). 100% client-side/visual, sem mudanca em `api/`, `lib/`, contratos de dados, cache ou Redis. Build verde antes e depois do merge. Auditoria aprovou sem bloqueadores (2 achados cosmeticos de baixissima severidade, nao-bloqueantes — ver CONTEXT.md, Licoes Aprendidas). **Ajuste jul/2026 (feedback do usuario, commit `ca4f036`, direto em main):** o pill parava de animar apos 3 ciclos e sumia sozinho apos um timeout de ~4s mesmo sem interacao — trocado por loop continuo enquanto o usuario nao arrastar a tabela de fato; dismiss agora acontece **somente** via scroll horizontal real. Pendente: validacao visual em iPhone real (ver CONTEXT.md, "Estado Externo").
- **Hardening batch — seguranca, estabilidade e performance (jul/2026 — branch `claude/code-review-feature-analysis-6qoly1`):** Lote de melhorias de infraestrutura originado de uma auditoria de codigo. **(1) Fix critico de auth:** `api/price.js` e `api/index-quote.js` usavam `if (!auth) return` — como `authenticate()` sempre retorna objeto (truthy), os dois endpoints estavam efetivamente SEM autenticacao; corrigidos para o padrao `if (!auth.ok)` dos demais handlers. **(2) Remocao de usuario completa:** `DELETE /api/users` agora apaga todas as chaves `portfolio:email:<hash>:*` via SCAN (antes so `:holdings` — transactions/caches/snapshots ficavam orfaos). **(3) Hardening de senha:** comparacao constant-time (`constantTimeEqual` movido para `lib/auth.js`) + rate limit por IP em Redis (10 falhas/15min → 429, fail-open). **(4) Auth misc:** refetch do JWKS do Google quando o `kid` do token nao esta no cache (rotacao de chave nao trava login por 1h); cache in-memory 60s do allowlist Redis; `lazyConnect: true` no ioredis; catch-all 500 responde `Internal error` generico (detalhe vai pro log). **(5) Concorrencia otimista:** PUTs de holdings/transactions aceitam `expectedSavedAt` e respondem 409 se outro device salvou no meio; cliente (`App.jsx`/`Transactions.jsx`) rastreia o `savedAt` lido e exibe toast/erro pedindo reload no conflito. **(6) Quotes:** cache Redis GLOBAL 60s por ticker + endpoint batch `GET /api/price?tickers=A,B,C` (max 60, concorrencia 4, FX prefetchado); Refresh All do `App.jsx` passou de N requests para 2 batch calls, com fallback pro loop legado. **(7) Parser testavel:** todo o parsing CSV/Fidelity extraido de `Transactions.jsx` para `src/lib/parsing.js` (modulo puro, ~890 linhas) + `test/fidelity-parser.test.mjs` com 25 casos de regressao cobrindo os bugs historicos (PR #95, #116, item 40, redemptions, foreign tax, INTEREST EARNED CASH, etc.). **(8) CI:** `.github/workflows/ci.yml` roda `npm install` + `npm run build` + todos os `test/*.test.mjs` em cada push. Build verde; 11/11 perf-history + 25/25 fidelity-parser.
- **Analytics batch — 5 features novas (jul/2026 — branch `claude/code-review-feature-analysis-6qoly1`):** Itens 1/3/4/6/8 da analise de produto de jul/2026, escolhidos pelo usuario. **(1) Snapshot mensal de Net Worth TOTAL:** rota `?resource=networth-history` em `api/contributions-history.js` (GET/PUT, chave Redis `:networth-history`); `App.jsx` grava automaticamente o `totalValue` live (TODOS os holdings, incl. Cash/Unallocated/BRA Fixed Income) uma vez por load com debounce de 5s; card colapsavel "Total Net Worth History" (bar chart mensal, gold) no rodape da tab Performance com empty state explicando a acumulacao organica. **(3) XIRR + Risk:** card "Risk & Drawdown" em `Performance.jsx` — KPIs XIRR (money-weighted), CAGR (TWR), Volatilidade anualizada, Sharpe (rf=0), Beta vs SPY e Max Drawdown (com datas pico→vale) + **(4a)** grafico underwater (% abaixo do pico, area vermelha). **(4b) Monthly Returns:** card heatmap ano×mes (TWR % por mes-calendario, celulas verde/vermelho com intensidade saturando em ±8%, coluna YTD composta) com scroll horizontal via `ScrollHintTable`. **(6) Invested vs Growth:** card com `ComposedChart` — area azul (valor do portfolio, `portfolioUSD`) vs linha gold tracejada (capital investido liquido acumulado das transactions); tooltip mostra Value/Invested/Gain; respeita `valuesHidden`. **(8) Portfolio Map:** treemap de holdings (`src/TreemapCard.jsx`, lazy — recharts fora do bundle principal, chunk proprio ~24KB) na tab Holdings abaixo do Rebalance: tile = valor atual, cor = variacao do dia (cinza para manuais/cash/bonds sem preco live), tooltip com valor mascarado por `valuesHidden`. **Infra:** toda a matematica nova vive em `src/lib/analytics.js` (modulo puro — wealthFromTwr, dailyReturns, annualizedVolatility, cagr, sharpeRatio, betaVsBenchmark, underwaterSeries, maxDrawdown, monthlyReturns, investedSeries, xirr/xirrFromTransactions) coberto por `test/analytics.test.mjs` (18 casos). Metricas since-inception, desacopladas do seletor de periodo. Cashflows BRL convertidos pela FX atual do ticker (aproximacao consciente, footnote na UI; txs sem FX puladas e contadas). Build verde; 54/54 testes (18 analytics + 25 fidelity-parser + 11 perf-history).
- **Analytics batch — follow-up: filtros, reposicionamento do treemap e aceleracao de tabs (jul/2026 — mesma branch):** Tres pedidos do usuario. **(A) Filtros:** cards Risk & Drawdown, Monthly Returns, Invested vs Growth e Total Net Worth History ganharam seletor de periodo proprio (`ANALYTICS_PERIODS` 6M/YTD/1Y/5Y/MAX, pills douradas, helper `sliceByPeriod`, metricas re-computadas client-side por janela; XIRR permanece since-inception, rotulado). Classe/ticker nesses cards vem dos chips globais ja existentes da pagina (documentado no rodape do card Risk). Card "Portfolio Map" ganhou filtro multi-select de asset class (pills + Clear) — sem filtro de ticker (cada tile ja e um ticker). **(B) Reposicionamento:** section do Portfolio Map movida para logo abaixo do bloco principal de holdings, acima do card "Filters & Sort" (antes ficava depois do Rebalance Suggestion). **(C) Aceleracao de Performance/Dividends:** (1) cache de sessao module-level (hash FNV do payload) para POST /api/perf-history (serie + composition) e /api/dividends em `Performance.jsx` e `Dividends.jsx` — revisitar a tab na mesma sessao e instantaneo, zero rede; (2) `warmUpTabCaches` em `App.jsx` (once-per-session, 3s apos load) preloada os chunks lazy e dispara os mesmos POSTs das tabs fire-and-forget, deixando o cache Redis do servidor quente antes da primeira visita; perf-history em sequencia (aquece candles compartilhados antes do composition). Build verde; 54/54 testes.
- **Indicador de versao do build no header — v1 (jul/2026 — merged via PR #131):** Label `commit · data` (fonte mono 9px, `T.textFaint`) a esquerda do botao "Refresh all" no masthead de `src/App.jsx`. Valor injetado em build-time por `vite.config.js` via `define: { __APP_BUILD__ }` lendo `VERCEL_GIT_COMMIT_SHA` (env automatica da Vercel) + data do build; builds locais mostram "dev". Motivacao: apos cada feature, confirmar visualmente qual commit esta de fato no ar (distingue "deploy nao aconteceu" de "Safari servindo cache velho"). **Nota:** esse push direto no `main` nao disparou deploy na Vercel — resolvido abrindo e mergeando o PR #131 pela UI do GitHub em vez de push direto (causa raiz nao diagnosticada).
- **Indicador de versao do build no header — v2, substitui v1 (jul/2026):** A pedido do usuario, trocado de hash de commit para semver legivel `v0.0.0`. `vite.config.js` le `"version"` de `package.json` (`readFileSync`/`JSON.parse`, sem dep nova) e injeta via `define: { __APP_VERSION__ }`. **Passa a exigir bump manual do `package.json` antes de cada merge/deploy** — instrucao adicionada ao Deploy Pattern do `CLAUDE.md` e ao workflow em `docs/CONTEXT.md`. Trade-off consciente: perde o "impossivel esquecer" do hash automatico, ganha legibilidade (numero que cresce com o produto, nao string opaca).
- **Portfolio Map — filtro de asset class vira dropdown unico (jul/2026, ajuste de UX, mesma branch):** `TreemapCard.jsx` trocou a linha de chips (um botao por classe) por um dropdown multi-select unico (`ClassFilterDropdown`) — mesmo padrao visual/interacao do `FilterMultiSelect` ja usado em `Dividends.jsx` (trigger + popover com checkboxes + Clear), duplicado localmente por convencao do projeto. Motivo: portfolios com muitas classes deixavam a linha de chips longa demais / quebrando em varias linhas.
- **SimpleFin Feed — Fase 3: heartbeat de sync + limpeza do scraper obsoleto (jul/2026 — commit `fbc9a15`, merged em main):** Fecha o plano `docs/plans/simplefin-fidelity-feed.md`. **(1) Heartbeat de sync no painel de Alerts:** nova funcao `refreshSimplefinHeartbeat()` em `src/App.jsx`, admin-only, chamada apos `refreshAlerts` no load. Consulta `GET /api/fidelity-pending?resource=status` (`{ ok, connected, lastSync, lastError, nextSyncAt }`) e dispara alerta se houve `lastError` na ultima tentativa ou se `lastSync` esta nulo/mais antigo que 48h — tres mensagens possiveis: erro real, nunca sincronizado, ou stale com contagem de dias. Reusa `mergeAlerts`/`setAlertLog` (novo `type: "simplefin_heartbeat"`), mas com `id` **estavel** (sem data embutida) — precisa remocao explicita do log quando o sync volta a ficar saudavel, diferente dos demais tipos de alerta (cujo id embute a data e "some" sozinho). **(2) Remocao do caminho scraper (nunca ativado em producao):** deletados `api/ingest-fidelity.js` (libera 1 slot de Serverless Function, 11/12) e `docs/plans/scraper/` inteiro; comentario de cabecalho de `api/fidelity-pending.js` atualizado para nao referenciar mais `ingest-fidelity.js`. `package.json` `1.3.1 -> 1.4.0`. Build (`npm run build`) e suite completa `test/*.test.mjs` (70 casos) verdes.
- **SimpleFin Feed — Auto-apply de Cash e Bank Bonds + Cash read-only em Holdings (jul/2026, commit `62161f1`, merged em main, v1.7.0):** Extensao pontual sobre as Fases 1-3 (nao uma nova fase do plano `docs/plans/simplefin-fidelity-feed.md`) — decisao explicita do usuario de abrir mao da revisao manual (Approve/Dismiss) especificamente para os dois Balance Updates do SimpleFin, Cash e Bank Bonds; Trades e Income continuam 100% manuais. **Supersede a Fase 1** (secao "Balance Updates" com Approve/Dismiss por linha, descrita acima): essa secao foi removida do card "Fidelity Import" — nao sobra nada para aprovar. `src/Transactions.jsx`: os pontos que computavam `freshBalance` (load on-mount e `runFidelitySync()`) agora chamam `onApproveFidelityBalance?.(c)` automaticamente para cada candidate fresco, sempre limpando `balanceCandidates` no staging remoto. Protecao contra double-apply via `appliedBalanceIdsRef` com chave composta `` `${candidate.id}:${candidate.proposed}` `` (nao so `id`, que e deterministico por conta+tipo e nunca muda entre syncs). Fix junto: `pruneUnchangedBalanceCandidates` comparava Bank Bonds contra `manualValue` (campo errado) em vez de `marketValueOverride`. `src/App.jsx`: novo campo `simplefinSyncedAt` + bloco "Last Synced (SimpleFin)" no accordion do holding Cash; nova prop `valueLocked` (`h.id === CASH_ID && isAdmin` — gate por admin, nao global) trava a edicao manual do valor de Cash; Target% continua editavel. Bank Bonds nao mudou (ja era read-only desde PR #93). Decisao consciente do usuario: Cash fica sempre travado independente da saude do sync (sem destrava automatica se o SimpleFin falhar/ficar stale) — correcao nesse cenario exige intervencao fora do app. Sem mudanca em `api/fidelity-pending.js`, sem bump de cache. **Auditoria (2 rodadas):** rodada 1 reprovou por bug bloqueante (`appliedBalanceIdsRef` usava so `id`, travando o candidate permanentemente apos a 1a aplicacao — atualizacoes reais de saldo em syncs seguintes eram descartadas silenciosamente); fix (chave composta) aprovado na rodada 2.
- **SimpleFin Feed — Auto-discard silencioso de "Electronic Funds Transfer Received" (jul/2026, sha `7a11f40`, merged em main, v1.7.1):** Transacoes Fidelity com description "Electronic Funds Transfer Received" caiam em `unmapped` (reason "unrecognized description") a cada sync, exigindo Dismiss manual repetido — a dedupe em `api/fidelity-pending.js` so compara contra o staging atual, nao contra historico de decisoes ja tomadas (dentro da janela de 44 dias, `SIMPLEFIN_WINDOW_DAYS`). Fix: `lib/simplefin-map.js` ganhou um terceiro padrao de exclusao silenciosa, `EXCLUDE_EFT_RECEIVED_RX = /^ELECTRONIC FUNDS TRANSFER RECEIVED/i`, aplicado em `mapOneTransaction()` no mesmo bloco de excludes ja existente (`EXCLUDE_CASH_CYCLE_RX`, `DISTRIBUTION_RX`) — nunca chega a `unmapped`, nunca aparece na UI. So cobre "Received", nao "Sent" — decisao explicita do usuario, ja que um EFT enviado pode representar saida de capital real que vale rastrear, diferente de um recebimento ja capturado pelo balance update de Cash. Teste novo em `test/simplefin-map.test.mjs` (bloco "excluded cycles") cobrindo o caso real `'Electronic Funds Transfer Received (Cash)'`.
- **Bank Bonds — Consolidacao Total Cost/Current Value entre Holdings e Position Performance (jul/2026, sha `400568e`, merged em main, v1.8.0):** Fecha o ponto cego #1 (ver "Observacoes levantadas" abaixo) — o usuario via 4 numeros divergentes para o mesmo holding Bank Bonds. Causa raiz: `computeBankBondsPrincipal` (App.jsx) e `computeBankBondsValueAt` (Performance.jsx) eram duas implementacoes independentes do mesmo replay de transacoes; redemptions do SimpleFin (sem CUSIP, so `description` como pseudo-ticker) que nao batiam com a compra original criavam posicoes orfas no agrupamento por ticker do Performance, inflando o Total Cost, e o Current Value la nunca usava dado real do SimpleFin (projecao sintetica de juros). Fix: novo modulo compartilhado `src/lib/bankBonds.js` unifica as duas funcoes numa unica fonte; `computeBankBondsValueAt` ganhou resolucao de ticker via `extractBondMeta` (`src/lib/parsing.js`) comparando issuer+coupon%+maturity contra as compras conhecidas, cobrindo bonds importados via CSV Fidelity (bonds manuais continuam sem match). Current Value do Position Performance passou a ler `marketValueOverride` real do SimpleFin quando disponivel; sem override, iguala ao Total Cost (gain/loss zero, decisao do usuario) em vez da projecao sintetica antiga. Novo `test/bank-bonds.test.mjs` (5 casos). 100% client-side, sem bump de cache. Ver detalhes completos em `docs/CONTEXT.md`.
- **SimpleFin Feed — Bank Bonds "sumindo" do fluxo: diagnostico + fixes (jul/2026, sha `fe72fbe`, merged em main, v1.8.1):** Item 3 da sessao de continuacao SimpleFin. Uma investigacao na mesma sessao levantou 4 causas candidatas para Bank Bonds nao aparecerem no fluxo; esta entrega resolve 2 como bugs reais, fecha 1 como falso-alarme (so copy) e deixa 1 documentada como decisao de produto. **(1) Falso alarme:** compra de bond nunca vira transacao automatica do SimpleFin (o feed nao estrutura trades) — ja caia corretamente em `unmapped`, nunca era descartada; fix foi so copy explicando o motivo na secao "Unmapped — needs review" de `src/Transactions.jsx`. **(2) Nao mexida:** janela fixa de 44 dias (`SIMPLEFIN_WINDOW_DAYS`) segue sem aumento — trade-off de mais chamadas a Bridge do SimpleFin vs risco de advisory de cap, documentada como limitacao conhecida. **(3) Bug real corrigido:** `computeBalanceCandidates` (`lib/simplefin-map.js`) excluia silenciosamente holdings de Bank Bonds com `market_value` invalido (`!isFinite(mv)`) da soma, sem sinalizar; agora retorna `{ candidates, skipped }` e os `skipped` sao mesclados no array `unmapped` ja exposto na UI. **(4) Bug real corrigido (o mais arriscado):** `applyFidelityBalanceUpdate` (`src/App.jsx`) descartava o update do SimpleFin com um toast invisivel (fluxo em background) quando o holding `bank-bonds-aggregate` ainda nao existia (usuario conectou SimpleFin mas nunca cadastrou uma transacao Bank Bonds manual) — o valor do SimpleFin era calculado e perdido, sem holding, sem alerta. Fix: `applyFidelityBalanceUpdate` agora auto-cria o holding com `manualValue: candidate.proposed` + novo campo `derivedFromTransactions: false`; novo guard em `applyBankBondsHolding` impede que o holding recem-criado seja zerado de volta no proximo load (sem transacoes reais, o principal recalculado seria 0) ate a primeira transacao Bank Bonds real ser cadastrada (holding "graduado" para `derivedFromTransactions: true`). Dois novos casos em `test/simplefin-map.test.mjs`; mudanca em `App.jsx` validada por revisao manual (nao testavel via harness). 100% client-side (exceto pequeno ajuste de shape em `lib/simplefin-map.js`), sem bump de cache. Ver detalhes completos em `docs/CONTEXT.md`.
- **SimpleFin Feed — Resolucao de ticker em transacoes de INTEREST (jul/2026, sha `747fcc2`, merged em main, v1.9.0):** Fecha a segunda metade do item 3 da sessao de continuacao SimpleFin ("interests nao estao batendo" — a primeira metade, "bonds nao retornando", ja tinha sido resolvida em `fe72fbe`/v1.8.1). Causa raiz: diferente de REDEMPTION (ja resolvido via `descKey`, sha `400568e`), a `description` de uma transacao INTEREST do SimpleFin traz so o nome do issuer (sem coupon%/maturity) — o `ticker` extraido era texto de issuer solto, sem CUSIP, que nao batia com a posicao real, gerando linha orfa em `Dividends.jsx`, dado desaparecendo em `Performance.jsx` e accrual duplicado/inflado em `buildBondProjections`. Fix em duas camadas: **(1) Auto-resolucao no backend** — `api/fidelity-pending.js` cruza o issuer do interest contra os HOLDINGS do mesmo sync do SimpleFin (description completa, com coupon%+maturity); quando exatamente 1 holding da conta bate com o issuer (normalizado) e resolve para um CUSIP conhecido do usuario via `descKey`, o ticker do interest e sobrescrito automaticamente. Match unico sem CUSIP conhecido ainda: ticker permanece o issuer, mas o evento ganha um campo `descKey` como dica. 0 ou 2+ matches: nada e resolvido (conservador, nunca por aproximacao). **(2) Dropdown editavel no card "Fidelity Income"** (`src/Transactions.jsx`) para quando a resolucao automatica falha/fica ambigua — usuario escolhe manualmente entre os CUSIPs de Bank Bonds ja conhecidos (dropdown, nao texto livre) antes de aprovar; aplicado em `approvePendingFidBond()` antes do dedupe e do push final. **Novo modulo compartilhado `lib/bond-meta.js`** (raiz do repo, sem dependencias de frontend, importavel por client e serverless): `extractBondMeta` (movida de `src/lib/parsing.js`, que agora importa e re-exporta com o mesmo nome — zero mudanca de contrato) + nova `buildKnownBondsByDescKey(transactions)`, que consolida 2 implementacoes duplicadas que ja existiam (uma privada em `src/lib/bankBonds.js`, outra inline em `ImportModal` de `src/Transactions.jsx`). `lib/simplefin-map.js` ganhou `bankBondHoldings(account)` (extrai o filtro `symbol === "" && description !== "CASH"` ja existente inline em `computeBalanceCandidates`, reaproveitado tambem pela logica de interest); `mapOneTransaction`/`mapSimplefinPayload` ganharam parametro opcional `knownBondsByDescKey` (default Map vazio, sem mudanca de comportamento quando omitido). Confirmado que **nenhuma mudanca foi necessaria em `src/Dividends.jsx`/`src/Performance.jsx`** — ja indexam por igualdade exata de string contra `ticker`. 5 novos testes em `test/simplefin-map.test.mjs` (match unico + CUSIP conhecido, match unico sem CUSIP, 0 matches, 2+ matches ambiguo, normalizacao de espacos/caixa) — suite completa 83/83. **Fora de escopo (decisao ja tomada):** correcao retroativa de `bondIncome` ja aprovado no passado com ticker errado (dado ja persistido); edicao de ticker para linhas `dividend`/`tax` no card Fidelity Income (ja resolvem corretamente hoje). Ver detalhes completos em `docs/CONTEXT.md`.
- **SimpleFin Feed — Matching de Bank Bonds sem CUSIP/descKey conhecido, Fatia 1 de 3 (jul/2026, sha `27cff99`, merged em main, v1.10.0):** Endereca o caso real do usuario: 11 Bank Bonds em carteira, so 4 com cupom%/vencimento salvos (os outros 7 foram cadastrados numa epoca em que a Fidelity ainda incluia CUSIP no CSV e nao exigiam metadata manual). Duas confirmacoes do usuario: SimpleFin nunca retorna CUSIP em nenhum campo, e a Fidelity recentemente parou de incluir CUSIP no CSV — as duas ancoras automaticas de que o mecanismo `descKey` (existente desde sessoes anteriores) dependia desapareceram, deixando 7 dos 11 bonds reais sem resolucao automatica possivel. **Solucao:** o unico dado estavel compartilhado pelos dois lados e o par (cupom%, vencimento), que o SimpleFin ja traz completo na description dos holdings de bond. **(1)** `extractBondHoldingsList(account)` (`lib/simplefin-map.js`) extrai `{ accountId, accountName, description, marketValue, descKey, couponRate, maturityDate, shortName }` de cada holding via `extractBondMeta`; propagado como `bondHoldings` no retorno de `mapSimplefinPayload` (antes so o `market_value` agregado sobrevivia, a lista individual era descartada). **(2)** Auto-bind automatico em `handleSync` (`api/fidelity-pending.js`): quando o `descKey` de um holding staged ja resolve via `knownBondsByDescKey` (cobre os 4 bonds ja cadastrados), o par e gravado automaticamente num novo campo `bondBindings` (objeto `descKey -> CUSIP`), sem acao do usuario. **(3)** Nova secao "Bond Matching" no card Fidelity Import (`src/Transactions.jsx`, irma de "Fidelity Income"/"Unmapped"): lista os holdings do SimpleFin ainda sem bind (os 7 reais do usuario), mostra description + cupom/vencimento quando parseavel, dropdown com CUSIPs de Bank Bonds ja conhecidos, botao "Confirm" grava via PATCH. **(4)** `bondBindings` tratado como dado confirmado, nao staging descartavel: sobrevive ao `DELETE /api/fidelity-pending`; no `PUT` e sempre merge raso (nunca replace). **(5)** `src/lib/bankBonds.js` (`resolvedTickerFor`, `computeBankBondsValueAt`, `listBankBondsTickers`) ganhou parametro opcional `bondBindings = {}` como fonte adicional de resolucao (fallback quando `knownBondsByDescKey` nao resolve) — default preserva 100% do comportamento anterior, `src/Performance.jsx` nao precisou de nenhuma mudanca. **(6)** Chave de fallback quando `descKey` e null (description nao parseavel): `raw:${description normalizada}`, duplicada server+client (padrao ja existente no projeto). 9 testes novos (7 em `test/simplefin-map.test.mjs`, 2 em `test/bank-bonds.test.mjs`); suite completa (83+) verde. Ver detalhes e decisoes tecnicas em `docs/CONTEXT.md`. **Hotfix critico logo apos o deploy (sha `8255619`, v1.10.1):** bug de temporal dead zone em `src/Transactions.jsx` (`useMemo` lendo `useState` declarado mais abaixo) quebrava a tab Transactions inteira (tela preta) — passou pela auditoria porque o ambiente sem browser nao pega erro de ordem de execucao em runtime. Corrigido fora do pipeline de 4 agentes, direto pela sessao principal. Ver `docs/CONTEXT.md` § Licoes Aprendidas.
- **SimpleFin Feed — Matching de Bank Bonds, Fatia 2 de 3: backfill de metadata (jul/2026, sha `faca800`, merged em main, v1.11.0):** Continuacao direta da Fatia 1 (v1.10.0). Quando existe um bind confirmado (`descKey -> CUSIP`) na tela "Bond Matching", preenche automaticamente `couponRate`/`maturityDate`/`shortName` E o campo de texto `notes` (formato `"4.20% | 07/08/2030"`, mesmo padrao que o regex de accrual em `computeBankBondsValueAt` ja espera) nas transacoes Bank Bonds do usuario que tem aquele CUSIP — resolve o caso real do usuario: 7 dos seus 11 bonds nao tinham cupom/vencimento salvos (cadastrados numa epoca em que a Fidelity ainda dava CUSIP no CSV), e sem preencher `notes` esses bonds continuariam "achatados" (sem projecao de juros) no grafico Composition Evolution. Regras confirmadas com o usuario: **nunca sobrescreve, so preenche campo vazio**; roda retroativo e automatico (cobre tambem binds ja confirmados antes desta fatia, sem botao). **Mudanca tecnica:** nova funcao pura `backfillBondMetadata(transactions, cusip, holdingDescription)` em `lib/bond-meta.js` — usa `extractBondMeta` pra extrair metadata da description do holding, preenche so campos vazios/null em toda tx `Bank Bonds` com ticker == cusip (buy e sell), retorna a mesma referencia de array quando nao ha mudanca real; novo `useEffect` de reconciliacao em `src/Transactions.jsx` sobre todas as entradas de `bondBindings`, com guards de assinatura de conteudo (`backfillContentSigRef`) e in-flight (`backfillInFlightRef`) pra prevenir loop de efeito — auditor validou manualmente que o ciclo converge em 2 disparos / 1 unico PUT ao servidor (build/testes nao pegam loop de efeito React sem browser, mesma licao do hotfix de TDZ v1.10.1). **Limitacao conhecida:** so funciona para holdings cuja description do SimpleFin parseia via `extractBondMeta` (formato `EMISSOR CD X.XXXXX% MM/DD/YYYY`); Treasuries/formatos diferentes ficam no-op (comportamento correto, nao bug) — o bind manual ainda funciona (tira da lista de pendentes), mas nao alimenta accrual. 7 novos testes em `test/bond-meta.test.mjs`; suite completa 97/97 verde. **Nota do auditor (nao bloqueante):** o `useEffect` de load de staging Fidelity tambem depende de `transactions`, entao cada persist do backfill dispara 1-2 GETs extras a `/api/fidelity-pending` por convergencia — comportamento pre-existente, so amplificado; vale observar em producao. **Fatia 3 (desambiguacao de INTEREST por valor esperado) foi avaliada e descartada na forma original nesta mesma sessao** — ver Pendentes/SimpleFin abaixo.
- **Bank Bonds — ID sintetico para bonds sem CUSIP (jul/2026, sha `8b05eed`, v1.12.0):** Continuacao da linha de trabalho de Bank Bonds SimpleFin (Fatias 1 e 2). Nem o CSV Fidelity nem o sync SimpleFin trazem mais CUSIP — em vez de exigir digitacao manual, o app gera um identificador determinístico a partir de cupom+vencimento. **Formato:** `<YYYYMMDD><cupom em pontos-base>`, sem prefixo/separador (ex: cupom 3,95% vencendo 2029-05-08 -> `20290508395`); vencimento sempre com 8 digitos fixos evita colisao por concatenacao com o cupom; cupom quebrado (ex: 4,125%) usa `x1000` como fallback (`202905084125`). **Por que funciona:** a resolucao de INTEREST/REDEMPTION do SimpleFin (v1.9.0/v1.10.0) depende so de `couponRate`/`maturityDate`/`shortName` na transacao, nao do formato do `ticker` — bonds com ID sintetico auto-resolvem contra eventos futuros do SimpleFin sem precisar da tela "Bond Matching". **Mudanca tecnica:** `lib/bond-meta.js` ganhou `generateSyntheticBondTicker(couponRate, maturityDate)` (funcao pura); `src/lib/parsing.js` (`parseFidelityCSV`) gera o ticker automaticamente para compra Bank Bonds sem CUSIP cuja description parseia e nao bate em `knownBondsByDescKey`, forcando `assetClass="Bank Bonds"` explicitamente (o ticker so-digitos de 11 chars nao casa `CUSIP_RX` nem `inferAssetClass`); `src/Transactions.jsx` (`TransactionForm`) ganhou link opt-in "Don't have a CUSIP? Generate ID from coupon + maturity" que expande mini-form (Coupon %, Maturity, Issuer opcional) com ticker editavel, e `handleSubmit` passou a incluir `couponRate`/`maturityDate`/`shortName`/`notes` no form de criacao manual (antes so o parser CSV e a edicao inline incluiam esses campos). **2 bugs bloqueantes corrigidos na auditoria (rodada 1 reprovou, rodada 2 aprovou):** os guards de REDEMPTION e INTEREST em `src/lib/parsing.js` exigiam que o ticker recuperado via `knownBondsByDescKey` casasse `CUSIP_RX` (9 chars) — um ticker sintetico (11 chars) era descartado silenciosamente, quebrando exatamente o caso de uso (juros nunca contabilizados, posicao nunca zerada no vencimento). Fix: flag `symbolFromHistory`/`symbolIFromHistory` seta so quando o symbol vem do lookup por descKey; guards passam a aceitar CUSIP real OU sintetico vindo do historico, mas ainda rejeitam Symbol cru invalido. Removido tambem um filtro redundante de `CUSIP_RX` no alerta de vencimento de bond (`src/App.jsx` `refreshAlerts`) que silenciaria o alerta para bonds com ID sintetico. 4 testes novos em `test/fidelity-parser.test.mjs`. **Limitacoes conhecidas:** nao migra os 11 bonds legados (decisao do usuario — continuam com CUSIP real + tela Bond Matching); colisao aceita quando dois bonds tem mesmo cupom+vencimento e emissores diferentes (nao existe nos dados atuais do usuario, ticker editavel como escape); so funciona para description parseavel via `extractBondMeta` (Treasury em formato diferente cai no comportamento pre-existente); gerador nao existe na edicao inline da tabela, so no form de criacao e no CSV. Ver `docs/CONTEXT.md` para a licao sobre validacoes de formato de ticker espalhadas pelo codigo.
- **Remocao do accrual estimado de Bank Bonds do Dividend History e de todos os calculos de dividendos (jul/2026 — merge `00988bc`, v1.14.1):** So dado real conta agora: pagamentos vindos do sync SimpleFin (`bondIncome`) e do import da Fidelity. Em `src/Dividends.jsx`: removido o step 3 (estimated accrual) de `buildBondEvents`, a funcao `accrueByFreqAsEvents`, e os helpers orfaos `minISO`/`addDaysISO`; removido o guard `if (e.source !== "estimated")` do calculo Y/Y (sem outra semantica alem de excluir estimados); removidos `hasRealBond`/`hasEstBond`; `bondLabel` virou constante fixa `"bond interest"`; removido o badge "EST" e seu tooltip no `DividendHistoryTable`. Em `src/Performance.jsx`: `buildBondEvents` reduzida ao "step 1" (so pagamentos reais), assinatura agora `buildBondEvents(bondIncome)` (diferente da assinatura em `Dividends.jsx`, que mantem `(transactions, bondIncome, todayISO)` — consequencia aceita da duplicacao por convencao do projeto); deletados como codigo morto `parseBondNotes`, `daysBetweenISO`, `minISO`, `freqFromDays`, `FREQ_DAYS`, `addDaysISO`, `accrueByFreqAsEvents`. Cards afetados (todos passam a mostrar so dado real): Income History KPIs (All Time/YTD/This Month), bar chart, Position Dividends, Dividends Monthly Y/Y, Dividends Monthly Map, Dividend History (`Dividends.jsx`); `Div TTM`, `Total Div`, `YoC %` e o `totalReturn` do grafico (`Performance.jsx`, onde os estimados inflavam os numeros silenciosamente, sem badge visivel). Efeito esperado: valores CAEM para quem tem Bank Bonds sem cobertura completa de pagamentos reais importados — comportamento desejado. **Card "Bond Projections" mantido intocado** (decisao explicita do usuario) com seu proprio badge "EST" — e forward-looking (proximos cupons futuros) e nunca entra em soma de "dividendo recebido"; e agora o unico lugar do app que usa estimativa de juros de bond. Nao tocados de proposito: `src/lib/bankBonds.js`, `src/AporteQuinzenal.jsx`, `api/dividends.js`, `src/Events.jsx` — sem mudanca de backend, sem bump de cache Redis, sem migracao (eventos estimados eram 100% client-side, nunca persistidos). `package.json` bump patch `1.14.0 -> 1.14.1`.
- **Remocao do card "Bond Projections" (jul/2026 — merge `038d6fa`, v1.14.2):** Continuacao imediata da entrega anterior (v1.14.1), que tinha mantido esse card como unica estimativa de juros de bond do app. O usuario reviu a decisao e pediu a remocao: **nenhuma estimativa de juros de bond permanece no app** — so dado real do SimpleFin (`bondIncome`) ou do import da Fidelity, sem excecao. Removidos de `src/Dividends.jsx`: a funcao `buildBondProjections` e todo o JSX do card (5º card colapsavel da tab, com badge "EST" proprio e rodape "Projected payments are estimates..."), o state `bondProjOpen`, o useMemo `bondProjections`, e tudo que ficou orfao depois disso — os helpers `parseBondNotes`, `daysBetweenISO`, `freqFromDays`, `FREQ_DAYS`, o step 2 de `buildBondEvents` (calibracao de `freqByCusip`, que existia exclusivamente para alimentar as projecoes, junto com `realByCusip`/`byDate`) e o `const todayISO`. `buildBondEvents` passou a ter assinatura `buildBondEvents(bondIncome)`, **agora identica a copia em `src/Performance.jsx`** — a divergencia de assinatura registrada na v1.14.1 deixou de existir. Diff liquido: 388 linhas removidas, 10 adicionadas, so em `src/Dividends.jsx` + bump de versao. Build verde, suite 140/140 sem regressao (nenhum teste cobria o card). Nenhuma mudanca de backend, de cache Redis ou de dado persistido.
- **Sync Fidelity movido pro Bulk Import + deteccao de delta de posicao para acoes/ETFs (jul/2026 — PR #138, sha `803c694`, merged em main, v1.14.0):** Implementada, auditada (APROVADA) e em producao. Duas mudancas em `src/Transactions.jsx`/`lib/simplefin-map.js`. **(1) UI:** botao "Sync Fidelity" movido do card "Sync & Automation" para uma nova aba "Sync" (admin-only) dentro do modal Bulk Import (`ImportModal`) — as filas de aprovacao (Trades/Income/Unmapped) permanecem no mesmo lugar, so o gatilho do sync mudou de posicao. **(2) Deteccao de delta de posicao para acoes/ETFs (feature nova):** nova funcao `stockPositionDeltas(account, netQtyByTicker)` em `lib/simplefin-map.js` — irma de `bondBuyTransactions`, mas para holdings com `symbol !== ""`. A cada sync compara as `shares` reportadas pelo SimpleFin contra o net qty conhecido (`computeNetQty`, agora exportada de `lib/simplefin-map.js`, antes vivia so em `src/App.jsx`) e sintetiza candidatos `buy`/`sell` na MESMA fila "Trades" ja existente (reaproveita `approvePendingFid`/`discardPendingFid`, sem UI de aprovacao nova). Ticker novo ou shares aumentaram -> `buy` a preco de custo medio do snapshot (`purchase_price`, fallback `cost_basis/shares`), com nota explicita de que e custo medio aproximado, nao o preco real da compra incremental (decisao do usuario). Shares diminuiram -> `sell` usando `market_value` como proxy de preco, com nota de que e estimativa (o feed nao traz preco de venda real) (decisao do usuario). Falta de preco/market_value, ou liquidacao total sem valor -> vao pra `unmapped` (needs review). **Idempotencia:** `simplefinId` = `sfstock-delta:<accountId>:<ticker>:<sharesNew>` — ancorado na posicao-ALVO do snapshot, NAO no `asOf`/balance-date (mesmo padrao ja usado por `bondBuyTransactions`, `sfbond-buy:<descKey>`, tambem sem data na identidade); usa `sharesNew` (nao `qtyDelta`) pra evitar colisao entre deltas de mesmo tamanho. Tabela de staged Trades ganhou coluna "Notes" com badges `DELTA` (delta derivado) e `BOND BUY` (bond sintetizado). **Limitacao conhecida:** liquidacao total onde o ticker desaparece INTEIRAMENTE dos holdings de uma conta nao e detectada como venda, porque `netQtyByTicker` e agregado entre contas — status quo, sem regressao, documentado em comentario no codigo. **Fase 2 (automacao 100% sem aprovacao humana, objetivo final do usuario) NAO implementada nesta feature, deliberadamente** — a deteccao e pura (sem side-effect no ato de detectar; aplicacao so via aprovacao manual existente), preparando o caminho; ver Pendentes. 12 testes novos em `test/simplefin-map.test.mjs` (total 51 passando, incluindo idempotencia cross-day); `test/perf-history.test.mjs` 11/11 sem regressao. **Auditoria (2 rodadas):** rodada 1 reprovou por bug de idempotencia (`simplefinId` ancorado no `asOf`/balance-date, que avanca a cada sync mesmo sem acao do usuario — duplicava o mesmo delta nao resolvido na fila a cada ~6h, risco de contar posicao em dobro); fix (ancorar na posicao-alvo) aprovado na rodada 2. Ver detalhes completos em `docs/CONTEXT.md`.

---

## 🔲 Pendentes

### Follow-ups — Sync Fidelity no Bulk Import + delta de posicao (acoes/ETFs) (jul/2026, PR #138, sha `803c694`, v1.14.0)
- **Fase 2 — automatizar aprovacao dos deltas de posicao (objetivo final do usuario):** analogo ao que ja foi feito com Cash/Bank Bonds no v1.7.0 (commit `62161f1`) — aplicar buy/sell direto, sem passar pela fila de aprovacao manual. Deliberadamente NAO feita nesta entrega — a orientacao do proprio usuario foi criar confianca no processo manual primeiro (varias semanas de sync real aprovado a mao) antes de automatizar.
- **Liquidacao total de um ticker que desaparece INTEIRAMENTE dos holdings de uma conta nao e detectada como venda.** `netQtyByTicker` e agregado entre todas as contas do usuario, entao a funcao nao enxerga "esse ticker sumiu desta conta especifica" — status quo, mesma limitacao de fundo ja documentada para o matching de Bank Bonds por conta (ver "Observacoes levantadas" em SimpleFin Feed, jul/2026).
- **Preco de compra do delta e custo medio aproximado, nao o preco real da transacao incremental** — o SimpleFin nao reporta series de trades individuais, so o snapshot de posicao (`purchase_price`/`cost_basis`). Aceito conscientemente pelo usuario; a nota "DELTA" na UI comunica isso.

### Follow-ups — ID sintetico para Bank Bonds sem CUSIP (jul/2026, sha `8b05eed`, v1.12.0)

- **Gerador de ID sintetico ausente na edicao inline da tabela de Transactions.** Existe no form de criacao manual e no parser CSV Fidelity; se o usuario precisar gerar um ID a partir da edicao inline de uma linha existente, tem que apagar e recriar. Nao priorizado.
- **Colisao de ticker entre dois bonds com mesmo cupom+vencimento mas emissores diferentes** gerariam o mesmo ticker sintetico (`shortName` distingue o emissor para efeito de match com SimpleFin, mas nao entra no ticker). Nao existe nos dados atuais do usuario; ticker permanece editavel manualmente como escape.
- **Migracao dos 11 bonds legados (com CUSIP real) para ID sintetico nao sera feita** — decisao explicita do usuario, continuam resolvendo via tela "Bond Matching".

### SimpleFin Feed — Bank Bonds matching, Fatia 3 de 3 — DESCARTADA na forma original (jul/2026)

Fatia 2 (backfill de metadata) foi **ENTREGUE** — ver Concluidos (jul/2026, sha `faca800`, v1.11.0).

Fatia 3 — desambiguacao de INTEREST por valor esperado quando 2+ holdings do SimpleFin batem o mesmo emissor — foi avaliada pelo planner na mesma sessao que entregou a Fatia 2 e **descartada na forma original**, com aval do usuario. Motivos: (1) o calculo de valor esperado depende da frequencia de pagamento (`freqByCusip`), que hoje e um chute pra CDs/Treasuries — a calibragem real so existe depois de 2+ pagamentos ja atribuidos ao CUSIP, problema galinha-e-ovo ja que a propria ambiguidade impede essa atribuicao inicial; (2) regra explicita do projeto: nunca resolver por aproximacao, um match errado silencioso e pior que nenhum; (3) ja existe o dropdown manual no card "Fidelity Income" pra corrigir interest ambiguo (sha `747fcc2`, v1.9.0), e juros de bond vem no maximo mensalmente — friccao baixa na pratica. **Achado registrado nesta avaliacao:** o match de INTEREST hoje e feito so pelo nome do emissor (`shortName`), nao pelo `descKey` completo (cupom%+vencimento) — a ambiguidade ocorre sempre que o usuario tem 2+ bonds do mesmo emissor (ex: os dois Treasuries 912810RN0/912810RK6 do usuario), nao so em casos raros. **Alternativa deixada em aberto para o futuro (nao priorizada):** usar o valor esperado so pra pre-selecionar um palpite no dropdown ja existente (nunca gravar sozinho, sempre exigindo confirmacao manual).

### Desktop responsivo — polish futuro — ENTREGUE (jun/2026 — commit c09a595)

- **Layout 2 colunas Allocation + Rebalance — REVERTIDO (jun/2026 — commit c50fcf1)**: o layout side-by-side (`sideBySide`, `flex row`) foi removido por preferencia de UX. Wrapper da secao Holdings voltou a `display: "block"` sempre — layout single-column empilhado em qualquer largura. `marginBottom`/`marginTop` fixos, sem condicional. `allocSectionW` usa `containerW - 32` sempre (donuts ocupam largura total do container). `windowWidth` state e `containerW` permanecem para outros usos (clamp dos donuts, font scaling). Mobile identico ao anterior.
- **Total Value font scaling — ENTREGUE**: `fontSize: 38` → `windowWidth >= 1024 ? 44 : 38`.
- **AporteQuinzenal 2 colunas — ENTREGUE**: Monthly Plan (flex 3) + This Month (flex 2) lado a lado em `windowWidth >= 768`. `windowWidth` state + resize listener adicionados em `AporteQuinzenal.jsx`. Contribution History permanece full-width.
- **Sub-views (Transactions, Performance, Dividends)**: KPI cards já usam `flexWrap`, tabelas têm scroll horizontal, charts usam `ResponsiveContainer` — comportamento adequado sem mudanças adicionais.


### US Bank Bonds — income real (follow-up do item 36)

- **Item 36 follow-up — ENTREGUE (jun/2026 — PR #87):** Import das linhas "INTEREST" do CSV Fidelity Account History como pagamentos reais de juros. `parseFidelityCSV` detecta linhas cuja Action contem "INTEREST" e cujo Symbol e um CUSIP, capturando `{ date, ticker, amount, kind: "interest", source: "fidelity" }`. Armazenados num **store separado** (campo `bondIncome` no blob de `/api/transactions`, fora do array de transacoes — nao toca `computeNetQty`/`dupKey`). O GET retorna `bondIncome`; o PUT preserva quando o body omite (read-modify-write). `computeBankBondsAccrual` mescla real + estimativa-no-gap e calibra `couponFreq` pela cadencia. Dedup por `date+ticker+amount`. Import aceita arquivos so-de-interest.
- **Bond interest em todos os cards + redemptions — ENTREGUE (jun/2026 — PR #88):** **(1) Redemptions:** linhas REDEMPTION/REDEEMED do CSV Fidelity com Symbol CUSIP viram transacoes **sell** (maturity do bond) — removem a qty/price compradas antes, zerando principal do holding agregado e parando o accrual na data. Price em branco cai para Amount/units (depois par 100); Quantity ausente cai para Amount. Redemption de non-CUSIP continua skipped. Flag `redemption: true` na transacao. **(2) Bond interest em todos os cards:** `buildBondEvents()` substitui `computeBankBondsAccrual`, emitindo eventos no mesmo shape dos dividendos de acoes — pagamentos reais (`source: "fidelity"`) + accrual estimado mensal (`source: "estimated"`, datado no fim do mes). O array `allEvents` (acoes + bonds) alimenta KPIs, bar chart, Position Dividends (YoC/Recovered funcionam para CUSIPs via cost basis), Dividend History (badge "EST" nas linhas estimadas; "—" em $/Share e Qty) e a tabela Y/Y. Comparacoes Y/Y % continuam excluindo eventos estimados (sem contraparte no ano anterior).
- **Entregue (PR #107):** Sub-header do card "Bond Projections" renderiza frequencia calibrada + coupon + maturity. Encerra o follow-up pendente de renderizacao do `freqByCusip`.
- **Bug fix — ENTREGUE (jul/2026 — commit d93cd87, merge b96922a, mergeado direto em main):** o accrual estimado gerava pagamentos "EST" fantasma — sizing sempre por mes de calendario fixo (ignorando `freqByCusip` calibrado, ja usado corretamente so pelo Bond Projections desde d6dc43b) e sempre incluia o periodo corrente incompleto datado em "hoje - 1 dia". Boundary bug adicional: `accrueFrom` nao avancava apos pagamento real, sobrepondo um "coto" residual. Ver detalhe no item acima (Item 36) e em CONTEXT.md (Decisoes Tecnicas + Licoes Aprendidas).

### Tab Contributions — Bank Bonds e aporte líquido

- **Filtro de Bank Bonds no cálculo de aporte investido — ENTREGUE (jun/2026 — PR #108 + fix fa48b02):** `computeHalfInvested` e `buildChartData` em `src/AporteQuinzenal.jsx` passaram a usar net qty de Bank Bonds por período (compras − redemptions). Rolagem de bond que venceu não infla mais o aporte quinzenal nem o histórico de aportes. Fix subsequente (fa48b02): campo "Dividends (last month)" agora soma também os juros reais de bonds (`bondIncome` com `kind: "interest"`) que o `POST /api/dividends` não retorna (endpoint só processa `kind: "dividend"`).

### Automacao de Download Fidelity

- **Item 38** 🧠 *Hard thinking — Opus recomendado*: **Automatizar download das transações da Fidelity.** Hoje o fluxo é manual: exportar CSV no site da Fidelity → importar via Import Modal. Caminhos possíveis: (a) Fidelity API oficial (existe mas exige OAuth + aprovação de developer account), (b) scraping autenticado via Playwright/Puppeteer em Vercel (complexidade alta, fragilidade), (c) email forwarding do extrato da Fidelity com parsing server-side (viável? ver item rejeitado de email parsing). Confirmar reachability e termos de uso antes de qualquer implementação. **Alta complexidade — mapear opções e riscos antes de codar.** Pipeline de staging/aprovação (`api/ingest-fidelity.js` + `api/fidelity-pending.js` + card "Fidelity Import" em Transactions) ficou implementado e dormente aguardando a fonte de dados (caminho (b), scraper Playwright/TOTP — nunca ativado).
- **Superseded (jul/2026) — feed SimpleFin:** usuário conectou a conta Fidelity via SimpleFin Bridge, eliminando a necessidade do scraper Playwright/TOTP (sem senha/2FA da corretora armazenada, sem GitHub Actions). Novo plano em [`docs/plans/simplefin-fidelity-feed.md`](./plans/simplefin-fidelity-feed.md) reaproveita o pipeline de staging/aprovação acima e cobre 5 alvos: Cash, valor de Bank Bonds, dividendos/juros/tax, transações buy/sell e transações de bonds. **Fase 0 — ENTREGUE:** rota `GET /api/fidelity-pending?resource=probe` (admin-only, read-only, sem tocar Redis) faz fetch direto no SimpleFin `/accounts` (janela 90d) e retorna um dump resumido (saldos, sample de holdings/transactions, erros) — sem endpoint novo (reaproveita `api/fidelity-pending.js`, dispatch por query-param, mesmo padrão de `api/contributions-history.js`, respeitando o limite de 12 Serverless Functions do Vercel Hobby). Card "SimpleFin Probe (admin)" em `src/Transactions.jsx`, visível só para `isUserAdmin(auth)`, com botão "Run Probe" exibindo o JSON bruto. Requer env `SIMPLEFIN_ACCESS_URL` no Vercel (setup manual do usuário, fora do escopo do código).
- **Fase 1 — ENTREGUE (jul/2026):** mapper puro `lib/simplefin-map.js` (`mapSimplefinPayload`, `isFidelityOrg`) traduz o payload SimpleFin já filtrado por Fidelity para `{ transactions, bondIncome, balanceCandidates, unmapped }` — 16 testes em `test/simplefin-map.test.mjs` (fixtures sintéticas) cobrindo dividend, bond interest (com e sem prefixo "as of"), redemption→sell, o ciclo INTEREST EARNED CASH/REINVESTMENT CASH (excluído), DISTRIBUTION (excluído), o holding sintético CASH (nunca soma no Bank Bonds), description desconhecida→unmapped e YOU BOUGHT/SOLD reconhecido mas roteado a unmapped (SimpleFin só reporta um `amount` total, sem qty/price estruturados — nunca inventa números). `api/fidelity-pending.js` ganhou `POST ?resource=sync` (admin-only, fetch real + mapper + merge/dedupe no `:fidelity-pending` — dedupe forte por `simplefinId` nativo + fallback `dupKey`/`bondKey`, throttle de 6h via timestamp no próprio blob, persiste erro/tentativa mesmo em falha) e `GET ?resource=status` (`{ connected, lastSync, lastError, nextSyncAt }`), além de um novo `PUT` (parcial, só substitui os arrays presentes no body, preserva metadados de sync) que os fluxos de approve/dismiss usam para remover só as linhas afetadas do staging em vez de zerar tudo. Card "Fidelity Import" em `src/Transactions.jsx` virou um grupo de 4 seções: Trades (existente), **Income** (dividend/interest/tax, checkboxes + Approve/Discard própria — antes era aprovado junto com trades sem listagem), **Balance Updates** (Cash/Bank Bonds propostos vs atuais, Approve/Dismiss por linha — Cash grava direto em `manualValue`; Bank Bonds grava um `marketValueOverride`/`marketValueOverrideAsOf` aditivo já que `manualValue` daquele holding é mantido em sync com as transações por `applyBankBondsHolding`) e **Unmapped** (somente leitura, nunca descartado silenciosamente). Botão "Sync Fidelity" + "Last sync" admin-only (mesmo gate do card SimpleFin Probe). `src/App.jsx` passou a passar `holdings` para `TransactionsView` e ganhou `applyFidelityBalanceUpdate` para aplicar Cash/Bank Bonds aprovados. Build + as 4 suites de teste (70/70: analytics + fidelity-parser + perf-history + simplefin-map) verdes. **Superseded (jul/2026, commit `62161f1`, ver "Auto-apply de Cash e Bank Bonds" na secao Concluidos acima):** a secao "Balance Updates" (Approve/Dismiss por linha) descrita aqui foi removida — Cash e Bank Bonds agora aplicam automaticamente, sem revisao manual.
- **Fase 2 — ENTREGUE (jul/2026, merge `eecf52c`, v1.3.1):** exibicao do `marketValueOverride`/`marketValueOverrideAsOf` de Bank Bonds no Dashboard, ultima lacuna da Fase 1. `ManualHoldingRow` (`src/App.jsx`) ganhou bloco condicional dentro do accordion expandido do holding (so aparece quando o campo esta setado) mostrando "Market Value (SimpleFin)" + valor mascaravel + delta (verde/vermelho, variavel local `marketValueDeltaColor` — nao reusa o `deltaColor` de drift de rebalanceamento, semantica diferente) + data "as of". Mudanca puramente de exibicao, sem endpoint/schema novo. Pendente para uma fase futura: resolver CUSIP real para bond interest/redemption via SimpleFin (hoje usa o texto da description como placeholder de ticker, sem CUSIP disponivel no feed) — decisao de design permanente (reconciliacao agregada), nao um gap temporario.
- **Fase 3 — ENTREGUE (jul/2026, commit `fbc9a15`, merged em main):** hardening + limpeza. Heartbeat de sync admin-only no painel de Alerts (`refreshSimplefinHeartbeat()` em `src/App.jsx`, consulta `GET /api/fidelity-pending?resource=status`, alerta se `lastError` presente ou `lastSync` nulo/mais antigo que 48h; `id` estavel exige remocao explicita ao voltar a saude, diferente dos demais tipos de alerta). Deletado o caminho scraper obsoleto nunca ativado em producao: `api/ingest-fidelity.js` (libera 1 slot de function, 11/12) e `docs/plans/scraper/` inteiro; `api/fidelity-pending.js` teve o comentario de cabecalho atualizado. `package.json` `1.3.1 -> 1.4.0`. Build + 70/70 testes verdes.
- **Auto-apply de Cash/Bank Bonds + Cash read-only — ENTREGUE (jul/2026, commit `62161f1`, merged em main, v1.7.0):** extensao pontual sobre as Fases 1-3 (nao uma Fase nova do plano) — decisao do usuario de eliminar o Approve/Dismiss manual dos Balance Updates de Cash e Bank Bonds (Trades/Income continuam manuais). Ver descricao completa na secao Concluidos acima. Falta so a Fase 4 (multi-usuario / claim de setup token).
- **Auto-discard de "Electronic Funds Transfer Received" — ENTREGUE (jul/2026, sha `7a11f40`, v1.7.1):** exclusao silenciosa (nunca chega a `unmapped`) de EFTs recebidos importados via SimpleFin. Ver descricao completa na secao Concluidos acima.
- **Bank Bonds "sumindo" do fluxo — diagnostico + fixes — ENTREGUE (jul/2026, sha `fe72fbe`, merged em main, v1.8.1):** Item 3 da sessao de continuacao SimpleFin ("matching de bonds — alguns nao estao retornando"). Investigou as 4 causas candidatas levantadas na mesma sessao (ver "Observacoes levantadas" abaixo) e corrigiu 2 bugs reais: `computeBalanceCandidates` (`lib/simplefin-map.js`) nao sinalizava mais holdings com `market_value` invalido excluidos silenciosamente da soma (agora vao para `unmapped` via novo campo `skipped`); `applyFidelityBalanceUpdate` (`src/App.jsx`) nao descartava mais o update do SimpleFin quando `bank-bonds-aggregate` ainda nao existia — agora auto-cria o holding (`derivedFromTransactions: false`, guard dedicado em `applyBankBondsHolding` ate a primeira transacao real ser cadastrada). Copy de "Unmapped — needs review" atualizada para explicar o caso de compra de bond novo (falso-alarme, ja funcionava). Janela de 44 dias mantida sem aumento (decisao de produto). Ver descricao completa na secao Concluidos acima e em `docs/CONTEXT.md`.
- **Resolucao de ticker em transacoes de INTEREST — ENTREGUE (jul/2026, sha `747fcc2`, merged em main, v1.9.0):** Fecha a segunda metade do item 3 ("interests nao estao batendo"). Auto-resolucao no backend cruzando issuer do interest contra holdings do mesmo sync (match unico + CUSIP conhecido resolve automaticamente); dropdown editavel no card "Fidelity Income" cobre os casos ambiguos. Novo modulo compartilhado `lib/bond-meta.js`. Ver descricao completa na secao Concluidos acima e em `docs/CONTEXT.md`.

**Observacoes levantadas em jul/2026 (mesma sessao do fix do EFT Received) — apenas pesquisa/investigacao, sem mudanca de codigo, nao priorizadas:**

- **Bank Bonds na tab Holdings ($10,453 no exemplo do usuario) e independente do valor de mercado real do SimpleFin.** `computeBankBondsPrincipal` (`src/App.jsx`) deriva o valor exibido do replay de transacoes manuais/importadas com `assetClass === "Bank Bonds"`. O valor de mercado do SimpleFin (soma de `market_value` de holdings com `symbol === ""`, exceto CASH) e capturado em `marketValueOverride` mas fica so como referencia num campo separado, visivel apenas ao expandir o holding — nunca entra no total agregado (comportamento mantido pela feature de consolidacao abaixo, decisao explicita do usuario). **Parcialmente enderecado (jul/2026, sha `400568e`):** o Total Cost/Current Value do card Position Performance agora batem com o "valor principal" e o "Market Value (SimpleFin)" da tab Holdings — ver secao Concluidos. **Ponto cego ainda aberto (nunca testado):** com multiplas contas Fidelity, cada conta gera seu proprio `balanceCandidate` de bank-bonds, mas todas escrevem no mesmo holding agregado — a ultima conta processada sobrescreve as anteriores em vez de somar.
- **Matching de bonds SimpleFin <-> holdings por CUSIP individual permanece fora de escopo — decisao de design permanente, ja documentada** (ver `docs/CONTEXT.md`, Fase 2 do SimpleFin Feed): o feed SimpleFin nao traz CUSIP, entao a reconciliacao e agregada (soma total vs. soma total), nao por posicao individual. Interest/juros nunca e somado ao principal do holding — fica em store separado (`bondIncome`), so aparece em Dividends/Yield. **Matiz adicionado (jul/2026, sha `400568e`):** para o proposito especifico de baixar o custo de uma compra quando ela vence (nao para reconciliacao completa de mercado), o Position Performance agora resolve o ticker de uma redemption individual via `descKey` (issuer+coupon%+maturity) quando o bond foi importado via CSV Fidelity — nao muda a decisao de design de nao usar CUSIP, so evita que a venda fique orfa e infle o custo agregado.
- **Inferir trades via diff pre/pos-sync do SimpleFin (comparar portfolio antes/depois do sync, tratar diferenca de valor como possivel compra/venda automatica): nao existe hoje nenhum mecanismo de snapshot/diff de holdings no codigo — seria uma feature nova, nao mapeada.** Limitacao de fundo: o SimpleFin nao expoe cost basis por holding individual (so `market_value` agregado sem CUSIP), entao so da pra comparar valor total, nao unidades/custo por bond especifico. Recomendacao levantada (ainda nao decidida com o usuario): tratar como alerta de revisao manual, nao lancamento automatico de transacao, dado o risco de dado incorreto.
- **4 causas candidatas para Bank Bonds "sumirem"/nao aparecerem no fluxo SimpleFin (levantadas nesta mesma sessao, item 3):** (1) compra de bond nunca vira transacao automatica do SimpleFin — falso-alarme, ja caia em `unmapped`; **ENTREGUE (copy)**. (2) janela fixa de 44 dias (`SIMPLEFIN_WINDOW_DAYS`) nao busca eventos mais antigos — decisao de produto, nao mexida (trade-off de mais chamadas a Bridge / risco de advisory de cap). (3) `computeBalanceCandidates` excluia holdings com `market_value` invalido sem sinalizar — **ENTREGUE**, bug real corrigido. (4) `applyFidelityBalanceUpdate` descartava o update quando `bank-bonds-aggregate` nao existia ainda — **ENTREGUE**, bug real corrigido (auto-cria o holding). Ver secao Concluidos ("Bank Bonds 'sumindo' do fluxo") e `docs/CONTEXT.md` para detalhes completos.

### Follow-ups — Bank Bonds consolidacao Holdings/Performance (jul/2026, sha `400568e`)

- **Validar em producao se a `description` do SimpleFin e byte-identica a "Symbol Description" do CSV Fidelity para o mesmo bond.** A resolucao de ticker de redemptions (`descKey` via `extractBondMeta`) assume compatibilidade de texto entre as duas fontes — forte probabilidade (mesma instituicao), mas nunca confirmado com um par real observado. Validar apos o primeiro sync real pos-deploy: Holdings "valor principal" deve bater com Position Performance "Total Cost" (Bank Bonds), "Market Value (SimpleFin)" deve bater com "Current Value".
- **Bonds Bank Bonds inseridos manualmente (sem `couponRate`/`maturityDate`/`shortName`) continuam sem resolucao de ticker individual.** So bonds importados via CSV Fidelity tem os metadados necessarios para o match via `descKey`. Nao e um bug — e uma limitacao documentada, coerente com a decisao permanente de nao ter CUSIP no feed SimpleFin. Sem prioridade definida; considerar so se o usuario passar a inserir bonds manualmente com frequencia.

### Follow-ups — Bank Bonds "sumindo" do fluxo (jul/2026, sha `fe72fbe`)

- **Multi-conta Fidelity ainda sobrescreve em vez de somar** (ponto cego identificado na sessao anterior, ver "Observacoes levantadas" acima) — permanece em aberto, nao fazia parte do escopo desta entrega (item 3 tratou especificamente das 4 causas de bonds "sumindo", nao do caso de multiplas contas). Nunca testado.
- **Janela de 44 dias do sync (`SIMPLEFIN_WINDOW_DAYS`) segue fixa** — decisao de produto confirmada nesta sessao, nao um item esquecido. Reconsiderar so se o usuario topar o trade-off de mais chamadas a Bridge do SimpleFin.

### Follow-ups — Resolucao de ticker em INTEREST (jul/2026, sha `747fcc2`)

- **Correcao retroativa de `bondIncome` ja aprovado no passado com ticker errado nao foi feita.** Cobre so transacoes novas a partir do deploy; dado ja persistido com o issuer como ticker fica como esta. Decisao ja tomada de nao fazer nesta rodada — se o usuario quiser, e uma sessao futura separada.
- **Edicao de ticker para linhas `dividend`/`tax` no card Fidelity Income nao foi adicionada.** Essas linhas ja resolvem corretamente hoje (o `ticker` extraido da description bate com o ticker real do ADR/acao) — fora de escopo por nao ser um problema observado.
- **Bonds inseridos manualmente (sem `couponRate`/`maturityDate`/`shortName`) continuam sem resolucao automatica de ticker de interest**, mesma limitacao ja documentada para redemptions (ver "Follow-ups — Bank Bonds consolidacao" acima) — coberto pelo dropdown manual no card "Fidelity Income".
- **Validacao empirica de producao ainda pendente:** assim como a resolucao de redemption via `descKey` (sha `400568e`), a resolucao de interest depende da `description` do holding SimpleFin ser compativel com a "Symbol Description" do CSV Fidelity — mesma pendencia de validacao pos-deploy, nao um gap novo.

### Follow-ups — Composition Evolution (jul/2026)

- **Testes formais para `computeCompositionSeries` e `computeBankBondsValueAt`:** o auditor validou com 3 casos ad-hoc (agregacao por classe, conversao FX BRA, candle ausente, input vazio) fora do suite; nao foram incorporados a `test/perf-history.test.mjs`. Nao-bloqueante, mas e um gap de cobertura para regressao futura.
- **Limpeza cosmetica de codigo morto:** campos `pos.lastBuyDate`/`pos.lastBuyNotes` em `positionRows` (`src/Performance.jsx`) ficaram calculados mas nao mais lidos apos a extracao do helper `computeBankBondsValueAt`.
- **Duplicacao de estilos inline em `CompositionCard`:** `cardHeaderStyle`/`cardBodyStyle` duplicados por o componente estar fora do closure de `PerformanceView` (forcado por escopo, nao preguica) — considerar consolidar se mais cards forem extraidos do closure no futuro.


